# إصلاح جذريّ: انزياح المطبوعة عن وسط الورقة — ط-٢١

**الحزمة:** `iomp-إصلاح-انزياح-الطباعة.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1524/1524 — ALL GREEN ✓` (+١ فحص) · **`firestore.rules` بلا تغيير** · **حجم النشر ١٨٩٬٤١٧ بايت مضغوطًا**.

---

## ١ · المشكلة

عند الطباعة كان الجدول **ينزاح إلى جانبٍ ولا يتوسّط الورقة**، تاركًا فراغًا كبيرًا في الجهة الأخرى (نحو ٣٠٪).

## ٢ · السبب الجذريّ (وجدته بالقياس)

التطبيق كان يكتب اتّجاه الصفحة **ناقصًا**: `@page{size:A4}` بلا كلمة `portrait/landscape`. فحين يكون إعداد الإدارة **عموديًّا** لكنّك تختار **أفقيًّا** في حوار طباعة المتصفّح (أو العكس)، ينشأ **تناقض**:

- المحتوى يُرصَف على عرض العموديّ = ‎210مم‎.
- الورقة الفعليّة أفقيّة = ‎297مم‎.
- فالمحتوى يملأ ‎210÷297 ≈ ٧٠٪‎ فقط ويُدفَع لجهة، والباقي ‎٣٠٪‎ فراغ — **بالضبط ما رأيتَه**.

غياب كلمة الاتّجاه كان يترك خيار «التخطيط» في حوار المتصفّح **حرًّا**، فيحدث التضارب.

## ٣ · الإصلاح الجذريّ

جعلتُ الاتّجاه **صريحًا دائمًا** في كلّ المطبوعات: `@page{size:A4 portrait}` أو `@page{size:A4 landscape}` حسب إعداد الإدارة. أثره:

- **يُقفَل «التخطيط» في حوار المتصفّح** على اختيار الإدارة ⇒ يستحيل التضارب ⇒ المحتوى يملأ الورقة ويتوسّط دائمًا، عموديًّا كان أو أفقيًّا.
- **مصدرٌ واحدٌ للاتّجاه = الإدارة** (إعدادات المخرجات ← اتجاه الصفحة). تريد أفقيًّا؟ اضبطه أفقيًّا هناك، فيُطبَع أفقيًّا مملوءًا بلا انزياح.

## ٤ · للتحكّم بعدد الصفحات (مهمّ)

لتصغير/تكبير المطبوعة **لا تستعمل «المقياس» في «إعدادات إضافية» بالمتصفّح** — فهو يُثبّت المحتوى أعلى-يسار الورقة (سلوك المتصفّح، لا يُعالَج بكود). بدلًا منه استعمل:

**الإدارة ← إعدادات المخرجات ← «حجم خط الطباعة»** (ميزة ط-١٩): رقمٌ أصغر ⇒ صفحاتٌ أقلّ، أكبر ⇒ أكثر — **ويبقى المحتوى ممتلئًا ومتوسّطًا** لأنّ عرض الجدول ثابتٌ ١٠٠٪. أبقِ «المقياس» في المتصفّح على **الافتراضيّ (Default)**.

---

## ٥ · ما لم يتغيّر

- الحسابات والأعمدة والتقارير دون مساس — إصلاحٌ في اتّجاه صفحة الطباعة فقط.
- الجداول تبقى ضمن عرض الورقة دائمًا (‏`table-layout:fixed` + أوزان الأعمدة).

---

## ٦ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.source.html` · `index.html` | `printPageCss` يكتب الاتّجاه صريحًا دائمًا (portrait/landscape) |
| `test_printfit.mjs` | تحديث P1/P4 (اتّجاه صريح) + P14 (إعداد الإدارة أفقيّ ⇒ landscape) — ٢١ |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدّ (١٬٥٢٣ ← ١٬٥٢٤) |

---

## ٧ · التركيب

1. نسخةٌ احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع.
3. `node build_release.js` ← `node build_harness.js` ← `node run_all_tests.mjs` = **`1524/1524 — ALL GREEN ✓`**.
4. ادفع إلى `main`.

**لا تغيير في أيّ حساب ولا قاعدة ولا صلاحيّة.**
