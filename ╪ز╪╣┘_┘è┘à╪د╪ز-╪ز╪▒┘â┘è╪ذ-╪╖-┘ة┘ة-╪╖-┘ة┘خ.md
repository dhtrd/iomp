# تعليمات تركيب الحزمة الختاميّة — ط-١١ إلى ط-١٦

**الحزمة:** `iomp-الكتالوج-والإعدادات-ط١١-ط١٦.zip` · **التاريخ:** ٢٦ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1391/1391 — ALL GREEN ✓` (٥٤ ملفًّا) · **`firestore.rules` لم يتغيّر منذ ط-١٠**

> هذه الحزمة **تراكميّة**: تحمل عمل ط-١١ حتّى ط-١٦ كاملًا، وتحمل معها ملفَّي ط-١٠ (`firestore.rules` و`firestore-rules.test.mjs`) احتياطًا إن لم تكن رفعت تلك الحزمة بعد. إن كنت رفعتها فالملفّان مطابقان لما عندك حرفًا بحرف، ورفعهما ثانيةً لا يضرّ.

---

## ١ · ما في الحزمة

| الملفّ | الوجهة | نوع التغيير |
|---|---|---|
| `index.html` | جذر المستودع — يستبدل القائم | **مُعدَّل — هذا هو ملفّ النشر** (٦٦٩٫٦ ك.ب) |
| `index.source.html` | جذر المستودع — **ملفّ جديد** | المصدر الموثَّق بتعليقاته (٧٧٣٫٢ ك.ب) — للصيانة لا للنشر |
| `build_release.js` | جذر المستودع — **ملفّ جديد** | أداة توليد `index.html` من `index.source.html` |
| `build_harness.js` | جذر المستودع — يستبدل القائم | خطّافات فحصٍ جديدة (`__cimp`, `__hist14`, `__quota`, `__act`, `__sets`) |
| `run_all_tests.mjs` | جذر المستودع — يستبدل القائم | تسجيل خمسة ملفّات فحصٍ جديدة (٤٩ ← ٥٤) |
| `test_catalog_import.mjs` | جذر المستودع — **جديد** | ٦٨ تأكيدًا — استيراد الكتالوج وتقريره |
| `test_catalog_scanadd.mjs` | جذر المستودع — **جديد** | ١٠٦ تأكيدات — تدفّقا ٤ و٥ |
| `test_history_runs.mjs` | جذر المستودع — **جديد** | ١٢٨ تأكيدًا — سجلّ المسح (المرحلة ٤) |
| `test_quota_lazy.mjs` | جذر المستودع — **جديد** | ٧٠ تأكيدًا — الحصّة والكفاءة (المرحلة ٦) |
| `test_settings_panel.mjs` | جذر المستودع — **جديد** | ٦٤ تأكيدًا — لوحة الإعدادات |
| `test_scanflow.mjs` | جذر المستودع — يستبدل القائم | ٩٣ تأكيدًا (كان ٩١) |
| `firestore.rules` | جذر المستودع — يستبدل القائم | **بلا تغيير منذ ط-١٠** (مرفق احتياطًا) |
| `firestore-rules.test.mjs` | جذر المستودع — يستبدل القائم | **بلا تغيير منذ ط-١٠** (مرفق احتياطًا) |
| `measure_rules_ceiling.mjs` | جذر المستودع — يستبدل القائم | أداة قياسٍ لا اختبار |
| `README.md` | جذر المستودع — يستبدل القائم | تحديث الأرقام (١٬٣٩١ فحصًا / ٥٤ ملفًّا) |
| `تعليمات-تركيب-ط-١١-ط-١٦.md` | مجلّد التقارير | هذه الوثيقة |
| `٩-تسليم-الكتالوج-والإعدادات-ط-١١-ط-١٦.md` | مجلّد التقارير | تقرير التسليم الكامل |

**لا تحذف أيّ ملفٍّ قائم.** الحزمة لا تُسقط شيئًا.

---

## ٢ · لماذا ملفّان للتطبيق؟ (`index.html` و`index.source.html`)

هذا قرارك المعتمَد: **«تجريد التعليقات عند التسليم»**.

- **`index.html`** = ما يُنشر. منه التعليقات فقط لا غير. **٦٨٥٬٧٠٦ بايت** خامًا، **١٨٠٬٥١٠** مضغوطًا.
- **`index.source.html`** = المصدر الموثَّق. **٧٩١٬٧٥٠ بايت** خامًا، **٢١٧٬٤٦٢** مضغوطًا.
- **الوفر:** ١٠٦٬٠٤٤ بايت خامًا · **٣٦٬٩٥٢ بايت مضغوطًا (١٧٫٠٪)** — وهذا ما يدفعه المتصفّح فعلًا.

**التعديل يكون دائمًا في `index.source.html`**، ثمّ:

```bash
node build_release.js        # يقرأ index.source.html ويكتب index.html
```

الأداة **ليست مصغِّرًا** عمدًا: المصغِّر يعيد تسمية المعرّفات ويحذف ما يظنّه ميتًا، وتكليفك يقول «حافظ على المنطق والمعرّفات ١٠٠٪». وهي **ليست تعبيرًا نمطيًّا** عمدًا كذلك: التعبير النمطيّ لا يفرّق بين تعليقٍ حقيقيّ وبين `//` داخل نصٍّ عربيّ أو قالبٍ نصّيّ أو تعبيرٍ نمطيّ، والملفّ يعجّ بها. فهي **محلِّلٌ نحويّ** (acorn) يحذف ما شهد النحوُ أنّه تعليق، ولا شيء غيره.

وفيها شبكتا أمان تعملان في كل بناء:
1. النصّ الناتج يُعاد تحليله نحويًّا — فلو انكسر شيء لتوقّف البناء.
2. تُجرَّد التعليقات من **المصدر والناتج معًا** ويُقارَن الاثنان بعد تطبيع المسافات: **يجب أن يتطابقا حرفًا بحرف**. فإن اختلفا طبعت الأداة موضع أوّل اختلاف وأنهت بخطأ. أي أنّ حياد النسختين **مبرهَنٌ بالبناء لا موعودٌ به**.

**والدليل العمليّ فوق البرهان:** شغّلتُ الحزمة الكاملة **مرّتين** — مرّةً على النسخة الموثّقة ومرّةً على نسخة النشر — والنتيجة `1391/1391` في الاثنتين، ملفًّا ملفًّا بالأرقام نفسها.

إن أردت الاكتفاء بملفٍّ واحدٍ في المستودع فارفع `index.html` وحده؛ لكنّي أنصح برفع الاثنين، لأنّ من يصون الملفّ بعد شهورٍ يحتاج التعليقات.

---

## ٣ · خطوات التركيب

1. **خذ نسخةً احتياطيّة** من `index.html` قبل الاستبدال.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp` (استبدالٌ مباشر).
3. `node build_harness.js` — يعيد بناء `harness.html` من `index.html` الجديد.
4. `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1391/1391 — ALL GREEN ✓`** (يستغرق ٩–١٠ دقائق).
5. ادفع إلى `main` ودع **CI** يحكم على القواعد — المحاكي محجوبٌ عندي، والـCI هو الحَكَم الوحيد كما اتّفقنا.
6. **لا تنشر القواعد يدويًّا.** `firestore.rules` لم يتغيّر أصلًا في ط-١١…ط-١٦.

---

## ٤ · قواعد الأمان — لا تغيير

**لم يُمَسّ `firestore.rules` في ط-١١ ولا ط-١٢ ولا ط-١٣ ولا ط-١٤ ولا ط-١٥ ولا ط-١٦.** آخر تعديلٍ عليه كان في حزمة ط-١٠.

القياس اليوم على الملفّ نفسه:

```
حجم النصّ: 28,833 / 262,144 بايت (11.0٪) — متبقٍّ 233,311
دوالّ: 27 · قواعد allow: 64
أثقل فرع (واقعيّ): 3,705 تعبيرًا — sessions/{sid} : update   ← بلا تغيير منذ ط-١٠
أقصى نداءات مستندات مميّزة: 3 / 10                          ← بلا تغيير
```

وتذكيرٌ بما وثّقناه سابقًا: هذا النموذج **حدٌّ أعلى متحفّظ** يفترض ألّا يقع قصرٌ للدارة في `&&` و`||`. والدليل أنّ الواقع أرحم: سبعة فروعٍ **قائمةٍ ومنشورةٍ وتعمل** تتجاوز ١٬٠٠٠ في نموذجي، والـCI أخضر. فقيمة الأداة **نسبيّةٌ لا مطلقة** — تُقاس بها الزيادة، ولا يُحكم بها على السلامة المطلقة. والزيادة هنا **صفر**.

---

## ٥ · ماذا يتغيّر في سلوك التطبيق بعد التركيب؟

خلافًا لحزمة ط-١٠ (التي كانت بناءً بلا توصيل)، **هذه الحزمة يظهر أثرها للمستخدم**:

| ما يراه المستخدم | أين | العلم الحاكم |
|---|---|---|
| تبويب «الكتالوج الرئيسيّ» ورفع ملفّ المنتجات وتقرير الاستيراد | القائمة الجانبيّة | `features.masterCatalog` — **مُفعَّل افتراضيًّا** |
| عند مسح باركودٍ غير موجودٍ في الجلسة وموجودٍ في الكتالوج: حوار «هذا الصنف غير موجود في جلسة الجرد هذه…» | شاشة العدّ | `catalog.autoAdd` |
| عند عدم وجوده في الكتالوج أيضًا: تنبيه «الباركود غير موجود في الكتالوج الرئيسيّ» ثمّ يواصل المسح فورًا | شاشة العدّ | — |
| سجلّ المسح يدمج المسحات المتتابعة للصنف نفسه في سطرٍ واحد `أحمد ×٣` | سجلّ المسح | `history.mergeRuns` |
| مقاييس الحصّة والقراءات والكتابات و`m` وقياس التخزين | الإعدادات | `quota.*` |
| **لوحة إعداداتٍ فيها ٤٣ مقبضًا** لكلّ قرارٍ اتُّخذ نيابةً عنك | الإعدادات (لمن يملك `perms.manage`) | — |

**ولم يتغيّر شيءٌ من الممنوعات:** لا قاعدة عملٍ للجرد، ولا حساب، ولا تقرير، ولا صلاحيّة، ولا خطوةٌ يدويّةٌ إضافيّة، ولا نقرةٌ بعد كلّ مسحة. الماسح يبقى جاهزًا للباركود التالي دائمًا — وهذا مُثبَتٌ بتأكيدٍ صريحٍ في `test_settings_panel.mjs` (المجموعة ز) وفي `test_catalog_scanadd.mjs`.

**وإن أردت إطفاء كلّ ذلك:** الإعدادات ← «الكتالوج الرئيسيّ» ← أطفئ `masterCatalog` ← احفظ. يعود التطبيق إلى سلوكه قبل ط-١٠ بلا رفعٍ ولا نشر.

---

## ٦ · التحقّق اليدويّ السريع (اختياريّ)

بعد `node build_harness.js`، افتح `harness.html` في متصفّح وشغّل في وحدة التحكّم:

```js
window.__sets.spec().map(s => [s.g, s.keys.length]);  // [['catalog',25],['history',6],['quota',12]]
window.__sets.val('history','history.mergeRuns');     // true
window.__quota.state();                               // {r: …, w: …}
window.__cimp.on();                                   // true  (علم الكتالوج)
await window.__cat.ensure();                          // {ok:true, …}
window.__cat.reads();                                 // يبقى صفرًا مهما بحثت
```

المهمّ: **`__cat.reads()` لا يتحرّك مهما بحثت** — البحث وقت المسح لا يمسّ Firestore إطلاقًا، تحقيقًا للمرحلة ٥.

---

## ٧ · ملاحظة أمانةٍ واجبة — الأسرار

فحص الأسرار على الحزمة كلّها نظيف: صفر مفاتيح AWS، صفر مفاتيح خاصّة، صفر حسابات خدمة، صفر رموز GitHub أو OpenAI، صفر ملفّات `.env`.

مطابقتان فقط لكلمة `client_secret`، وكلتاهما سليمة:
- في `node_modules/playwright-core` — اسم حقلٍ في مخطّطٍ داخل تبعيّةٍ لا تُرفع أصلًا.
- في `cloud_backup.mjs` — يقرأ من `process.env.DROPBOX_APP_SECRET`، أي **لا قيمة في الشيفرة**، وهذا هو النمط الصحيح.

وفي `index.html` القائم تطابقٌ واحدٌ هو **إعداد Firebase للويب** الموجود منذ اليوم الأوّل — مُعرِّفٌ عامٌّ بحكم التصميم يُشحن إلى كلّ متصفّح، والحماية الحقيقيّة على قواعد Firestore لا على إخفائه. **لم أُضِفه ولم ألمسه ولم أطبع قيمته في أيّ مكان.** أذكره لك لأنّ الشفافيّة أولى، لا لأنّه ثغرة.

---

## ٨ · الرجوع للخلف

ثلاث درجاتٍ حسب ما تريد:

1. **إطفاءٌ فوريّ بلا رفع:** الإعدادات ← أطفئ `masterCatalog` ← احفظ. يختفي الكتالوج وحوار الإضافة من كلّ الأجهزة خلال ثوانٍ.
2. **إرجاع سلوكٍ واحد:** كلّ مفتاحٍ من الـ٤٣ له مقبضٌ في الإعدادات، وزرّ «إرجاع الافتراضيّات» في كلّ بطاقة. مثلًا إطفاء `history.mergeRuns` يعيد سجلّ المسح إلى سطرٍ لكلّ مسحة كما كان — **والكمية لا تتغيّر**، وهذا مُثبَتٌ باختبارٍ صريح.
3. **إرجاعٌ كامل:** استبدل `index.html` بنسختك الاحتياطيّة، واحذف ملفّات الفحص الخمسة الجديدة، وأعِد `run_all_tests.mjs` و`build_harness.js`. `firestore.rules` لا يحتاج شيئًا لأنّه لم يتغيّر. وما كُتب في `catalogChunks` و`catalogMeta` و`catalogImports` يبقى في قاعدة البيانات بلا ضرر — لا شيء يقرؤه بعد الإرجاع.
