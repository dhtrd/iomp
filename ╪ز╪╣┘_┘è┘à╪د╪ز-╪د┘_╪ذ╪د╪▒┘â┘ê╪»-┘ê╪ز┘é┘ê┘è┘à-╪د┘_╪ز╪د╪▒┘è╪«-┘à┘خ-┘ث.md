# تعليمات: تحسينات قارئ الباركود + تقويم «تاريخ الجرد» (تقويم ٣) — م٦-٣

## أولًا — قارئ الباركود
1. **تنبيه عند الصنف غير الموجود**: عند مسح باركودٍ ليس في الجلسة، تظهر **رسالة تنبيه** واضحة (إضافةً لصوت الخطأ): «⚠ الصنف غير موجود في الجلسة: … — أضِفه بـ«➕ صنف يدوي» ثم امسحه».
2. **التعرّف الفوري بعد الإضافة**: عند فتح «➕ صنف يدوي» بعد مسحٍ مجهول، **يُعبّأ حقل الباركود تلقائيًّا** بالكود الممسوح؛ وبمجرد الحفظ يدخل الصنف قائمة العد **فورًا**، فيتعرّف عليه الماسح في المسحة التالية مباشرةً (دون انتظار مزامنة).
3. **لا تراكم «رقم فوق رقم»**: كل مسحة **تُفرِّغ حقل البحث** بعد معالجتها — سواءٌ سُجِّل العدّ أو ظهر تنبيه «غير موجود» — فلا تتراكم الأكواد الممسوحة فوق بعضها. والبحث النصّي العربي العادي يبقى كما هو (لا يُفرَّغ ولا يُنشئ عدّة).
4. **تسجيل تلقائي بالماسح الخارجي**: مسح كودٍ في حقل البحث + Enter يسجّل **عدّة ١+ تراكميًّا بصوت** دون الحاجة لزر «وضع الماسح» (يبقى «وضع الماسح» متاحًا كما هو). المطابقة متسامحة (تجاهل الأصفار البادئة/الباركود/المسافات).

> ملاحظة: العدّ **تراكمي** كما هو مطلوب في الجرد — كل مسحة = +١ فوق الإجمالي (١، ٢، ٣…). التفريغ المذكور هو تفريغ **حقل البحث** لا العدّ.

## ثانيًا — تقويم «تاريخ الجرد» (اعتماد «تقويم ٣»)
- استُبدل منتقي التاريخ بـ**تقويم منسدل من حقل (تقويم ٣)**: تنقر الحقل فينسدل تقويم كامل، وتنتقل بين **الأيام ← الأشهر ← السنوات** بالنقر على العنوان، وبالأسهم بين الأشهر ونطاقات السنوات — بأسماء عربية، واليوم المحدَّد بالنيلي والحالي بإطار.
- ظاهر في **لوحة «👥 أعضاء اللجنة»** بشاشة التقرير وفي **نافذة الطباعة**، ويقود متغيّر **{date}** في المحاضر (تاريخ يدوي ← بدء الجرد ← الإنشاء، بلا تاريخ اليوم)، ويُحفَظ أفضل-جهد على الجلسة.

## الملفات في هذه الحزمة (استبدل/أضف في جذر المستودع dhtrd/iomp)
| الملف | الحالة |
|---|---|
| `index.html` | **استبدال** — إصلاحات الباركود + تقويم تاريخ الجرد المنسدل |
| `build_harness.js` | **استبدال** — خطاطيف فحص جديدة |
| `run_all_tests.mjs` | **استبدال** — أُضيف `test_scanfix.mjs` |
| `test_scanfix.mjs` | **جديد** — ١٨ فحصًا للباركود (تنبيه/تعرّف فوري/عدم التراكم/التسجيل/المطابقة) |
| `test_printsummary.mjs` | **استبدال** — مواءمة فحص التاريخ مع التقويم المنسدل |
| `README.md` | **استبدال** — تحديث الأرقام (٨٢٨ فحصًا / ٤٧ ملفًا) |

## خطوات الرفع
1. ارفع الملفات إلى الجذر واعتمد (Commit) إلى main.
2. CI: فحوص التطبيق المتوقعة **٨٢٨/٨٢٨** + فحوص القواعد **١٠٠/١٠٠** ثم النشر.

## ⚠️ قواعد الأمان
**لم يتغيّر `firestore.rules`** — لا نشر في وحدة تحكم Firebase.

## أرقام الفحوص
- المحلي: **TOTAL: 828/828 — ALL GREEN ✓** (٤٧ ملفًا؛ صفر انحدار)
- CI قواعد الأمان: بلا تغيير (100/100)
