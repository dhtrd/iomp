# تعليمات تركيب م٨ — بطاقة «آخر صنف مُمسوح» تصير معاينةً حيّةً ثابتة

**الحزمة:** `iomp-البطاقة-الثابتة-م٨.zip` · **التاريخ:** ٢٦ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1460/1460 — ALL GREEN ✓` (٥٤ ملفًّا · +١٩ فحصًا) · **`firestore.rules` بلا تغيير**

> هذه الحزمة تنفّذ التحسين الذي وافقت عليه: بطاقة النتيجة تحت حقل المسح لم تعد إيصالًا عابرًا يختفي بعد ٣٫٢ ثانية، بل **معاينةٌ حيّةٌ ثابتة** بحقولها الثمانية، وفرقٌ ملوّنٌ حيّ، وزرّ إغلاق. **بلا تغيير قاعدة عملٍ أو حساب.**

---

## ١ · ما تغيّر في السلوك (كما يراه المُجرِد)

- **لا تختفي تلقائيًّا.** كانت تختفي بعد ٣٫٢ث (نجاح) أو ٦ث (خطأ) — أُزيل المؤقّت. تبقى حتّى:
  1. يضغط **(×)** — الإغلاق اليدويّ الوحيد.
  2. يمسح صنفًا آخر ⇒ **تُستبدَل محتوياتها فورًا** بلا تلاشٍ ولا حالةٍ فارغة.
- **ثمانية حقول** (كانت خمسة): اسم المنتج · الباركود · **الرصيد الدفتريّ** · المضاف بهذا المسح · الكمية الفعلية · **الفرق** · **حالة الجرد** · حالة المسح.
- **الفرق حيٌّ وأقوى تمييزًا:** رقمٌ كبيرٌ ممتدّ العرض، بثلاثة أنماطٍ متمايزة — **موجب** أخضر (زيادة) · **سالب** أحمر (عجز) · **صفر** محايد (مطابق). يُحسب لحظيًّا مع كلّ مسحة (١٣→١٤ ⇒ +٣→+٤).
- **إعادة مسح الصنف نفسه**: تحديثٌ موضعيٌّ للفعليّ والفرق فقط — بلا وميضٍ ولا إعادة بناء.
- **زرّ ×**: يُغلق البطاقة ويُعيد التركيز لحقل المسح فورًا (الماسح جاهز).

---

## ٢ · ملاحظة صحّةٍ منفَّذة: الجلسة العمياء

في الجلسة العمياء (`curSess.blind`) تُخفي البطاقة **الرصيد الدفتريّ والفرق وحالة الجرد** (لأنّها تُشتقّ من الدفتريّ)، وتكتفي بالاسم والباركود والمضاف والفعليّ وحالة المسح — كي لا تسرّب ما يُخفيه العمى. مثبَّتٌ بالفحوص ف١٣–ف١٦.

---

## ٣ · الأداء (مقيسٌ لا مُدّعى)

- **صفر Firestore:** البطاقة تقرأ الحالة المحلّيّة (`it.book` و`curCounts`) فقط. المسح يكتب كما كان (سلوكٌ قائم)؛ البطاقة تضيف **صفر** قراءةٍ وصفر كتابة. مسبار `probe_scanpanel.mjs` يؤكّد `storeKeysDelta: 0` لخمسمئة عرضٍ متتالٍ.
- **تحديثٌ موضعيٌّ أرخص بمراتب:** إعادة المسح تلمس عقدًا نصّيّةً بدل إعادة البناء — المسبار يقيس النسبة (`speedup`).
- **إزالة المؤقّت توفيرٌ لا كلفة.**
- **لا إعادة رسمٍ كاملة:** البطاقة عنصرٌ واحدٌ منفصلٌ عن `renderCountList`.

*(الأرقام المقيسة على جهازك في تقرير التسليم المرفق.)*

---

## ٤ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.html` · `index.source.html` | البطاقة الثابتة (CSS + `scanPanel` + `spClose` + تمرير `book`) |
| `build_harness.js` | تحديث خطّاف `__scanPanel` للبنية الجديدة |
| `test_scanflow.mjs` | تحديث مجموعة «س» + **مجموعة «ف» جديدة** (١١٢ بدل ٩٣) |
| `test_catalog_scanadd.mjs` | تحديث تسمية حقلٍ واحد (١٤٢ كما هي) |
| `probe_scanpanel.mjs` | **جديد** — مسبار قياسٍ لا اختبار |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدد (١٤٤١ ← ١٤٦٠) |
| `تعليمات-تركيب-م٨-البطاقة-الثابتة.md` · `تقرير-تسليم-م٨.md` | هذه الوثيقة وتقرير التسليم |
| تقارير المراجعة والنموذج والمخطّط (م٨) | للمرجع |

---

## ٥ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp` (استبدالٌ مباشر).
3. `node build_harness.js` ثمّ `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1460/1460 — ALL GREEN ✓`**.
4. ادفع إلى `main` ودع CI يحكم.

---

## ٦ · ما لم يتغيّر

لا قاعدة عملٍ، ولا معادلة، ولا تقرير، ولا صلاحيّة. البطاقة **مرآةٌ** لِما هو محسوبٌ أصلًا (`computeVariance` بلا مساس). الفرق = الفعليّ − الدفتريّ، مطابقٌ لحساب التقارير. المعرّفات وRTL والطباعة بلا مساس، وصفر أثرٍ على أداء المسح، والماسح يبقى جاهزًا دائمًا.
