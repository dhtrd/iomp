# تسليم — «من أضاف» في التقرير: تجميع مسحات التتابع بدل ١+١

**الحزمة:** `iomp-إصلاح-من-أضاف.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1483/1483 — ALL GREEN ✓` (٥٤ ملفًّا) · **`firestore.rules` بلا تغيير**

> تعديلٌ في **عرض** عمود «من أضاف / تفاصيل العدّات» فقط. **لا تغيير في أيّ حساب، ولا في الكميّات، ولا في الفروقات، ولا في المجاميع.**

---

## ١ · المشكلة

في التقرير، كانت مسحات الباركود المتتالية للصنف نفسه (تتابعُ عدٍّ واحد) تُعرض في «من أضاف» **مفكوكةً إلى ١+١**:

> عبدالكريم الضيفي: 2 (1 + 1)

بينما شاشة العدّ تعرضها **مجموعةً** (٢ للعدّاد). أردتَ أن يطابق التقريرُ شاشةَ العدّ.

**السبب (بالشيفرة):** الدالّة `repAgg` كانت **تفكّ التتابع المدموج** إلى أجزاءٍ مفردة (`per=q/n` مكرّرًا `n` مرّة) — قرارٌ من ط-١٤ ليطابق التقرير ما قبله حرفًا بحرف. فمسحتان متتاليتان (سطرٌ واحدٌ كميّته ٢) كانتا تظهران `(1 + 1)`.

---

## ٢ · الإصلاح

الآن **لا يُفكّ التتابع**: كلّ سطرٍ — سواءٌ تتابعٌ مدموجٌ ممسوح، أو إضافةٌ واحدة — يُسهم بجزءٍ واحدٍ **بكميّته كما هي**. النتيجة تطابق شاشة العدّ:

| الحالة | قبل | بعد |
|---|---|---|
| مسحتان متتاليتان (تتابع) | `عبدالكريم: 2 (1 + 1)` | **`عبدالكريم: 2`** |
| ثلاث مسحات متتالية | `المالك: 3 (1 + 1 + 1)` | **`المالك: 3`** |

**وما لم يتغيّر — تفصيل الإضافات المنفصلة الحقيقيّة يبقى** (وهو ميزةٌ للمراجعة طلبتَها سابقًا): إضافتان متمايزتان (مثلًا يدويّ ٤ ثمّ ٨) تبقيان مفصّلتين:

> مجاهد الضيفي: 12 (4 + 8)

أي: التتابع المدموج (مسحاتٌ متتالية) يُجمع؛ والإضافات المنفصلة الفعليّة تُفصّل. تمامًا كالمنطق في شاشة العدّ.

**الصيغة:** أبقيتُ شكلك المعتمَد **«الاسم: الإجمالي»** (اخترتَه). يسري على الشاشة والطباعة والتصدير المفصّل معًا (مصدرٌ واحد `whoDetail`).

---

## ٣ · لماذا هذا آمنٌ تمامًا

- **صفر تغييرٍ في الحساب:** إجماليّ كلّ عدّاد (`q`) يُجمع كما كان؛ تغيّر فقط **كيف يُعرض التفصيل** بين قوسين. الكميّة الفعليّة والفرق والقيمة والمجاميع بلا مساس.
- **مصدرٌ واحد** (`repAgg`/`whoDetail`) يخدم الشاشة والطباعة والتصدير — فلا تتضارب النسخ.
- `firestore.rules` والمعرّفات وRTL بلا مساس.

*مثبَّتٌ بالفحوص:* `test_history_runs` (ز١–ز٤): التتابع المدموج جزءٌ واحدٌ بكميّته، والمنفصلة تبقى `(1 + 1 + 1)`؛ و`test_viewfinance` (F12): تفصيل الإضافات المنفصلة `مجاهد الضيفي: 12 (4 + 8)` باقٍ.

---

## ٤ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.html` · `index.source.html` | `repAgg`: إيقاف فكّ التتابع (سطرٌ واحد) |
| `test_history_runs.mjs` | تحديث ز٢/ز٣/ز٤ للسلوك الجديد (١٣٤ كما هي) |
| `تعليمات-إصلاح-من-أضاف-تجميع-التتابع.md` | هذه الوثيقة |

*(عدد الفحوص ثابتٌ ١٤٨٣ — عُدِّلت تأكيداتٌ قائمة، ولم تُضَف فحوصٌ جديدة.)*

---

## ٥ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp`.
3. `node build_harness.js` ثمّ `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1483/1483 — ALL GREEN ✓`**.
4. ادفع إلى `main`.

---

الخطوة التالية باختيارك: **تنفيذ فلتر الفئات المتعدد بالنمط B** الذي اعتمدتَه.
