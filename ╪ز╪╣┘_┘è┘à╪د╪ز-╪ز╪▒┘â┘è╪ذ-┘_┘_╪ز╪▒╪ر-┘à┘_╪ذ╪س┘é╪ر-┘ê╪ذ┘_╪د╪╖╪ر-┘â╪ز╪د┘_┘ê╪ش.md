# توحيد الفلترين المنبثقين + بلاطة «من الكتالوج» — تركيبٌ وتشغيل · ط-١٨

**الحزمة:** `iomp-فلترة-منبثقة-وبلاطة-كتالوج.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1516/1516 — ALL GREEN ✓` (٥٥ ملفًّا) · **`firestore.rules` بلا تغيير** · **حجم النشر ١٨٨٬٦٦١ بايت مضغوطًا** (تحت سقف ٢٠٠٬٠٠٠).

---

## ١ · التعديلان المنفَّذان

### التعديل الأوّل — توحيد فلترَي «الفئات» و«الحالات» (منبثقان متعدّدان)

جعلتُ الفلترين بالنمط نفسه الذي طلبته: **كلاهما زرٌّ يفتح منبثقًا** (بمظهر قائمة «كل الحالات» نفسه)، **وكلاهما متعدّد الاختيار**.

- **فلتر الفئات:** كان لوحةً ظاهرةً دائمًا، صار **زرًّا منبثقًا** فيه بحثٌ + قائمة صناديق متعدّدة + «اختر كلّ النتائج» + «مسح الكلّ». (Enter = اختيار كلّ النتائج المطابقة — كما اعتمدته.)
- **فلتر الحالات:** كان **أحاديّ** الاختيار، صار **متعدّدًا** بالمنبثق نفسه — تختار «العجز» و«الزيادات» معًا مثلًا، والنتيجة **اتّحادها** (OR). لا اختيار = كلّ الحالات.
- **زرّ كلّ فلترٍ يعرض الملخّص:** «كلّ الفئات/الحالات» بلا اختيار · اسم الخيار عند اختيار واحد · «N فئات/حالات» عند التعدّد.
- **لوحة المفاتيح كاملة** في المنبثق: Enter (كلّ النتائج) · ↑↓ · مسافة (تبديل المظلَّل) · Backspace (يزيل آخر مختار) · Esc (يمسح البحث ثمّ يغلق) · نقرةٌ خارج المنبثق تغلقه.
- المكوّن **واحدٌ مُعاد استخدامه** للفلترين، ويعيد استخدام مظهر القوائم العصريّة (`.msel`) نفسه في النظام.

### التعديل الثاني — بلاطة «من الكتالوج»

أضفتُ **بلاطةً** في ملخّص التقرير تَعُدّ **الأصناف المُضافة من الكتالوج** (`manual && src==='catalog'`):

- **للفلترة والعرض فقط** — نقرةٌ عليها تعرض أصناف الكتالوج وحدها (كبقيّة البلاطات القابلة للنقر)، وهي أيضًا خيارٌ «من الكتالوج» ضمن فلتر الحالات.
- **دون تغيير باقي التقرير** — الأصناف تبقى ظاهرةً بحالتها الطبيعيّة في العرض غير المفلتر؛ البلاطة إضافةٌ لا تعديل.
- **ظاهرةٌ للمطّلع ولكلّ المستخدمين** (بلاطة عدٍّ لا تتطلّب صلاحيّةً ماليّة).

---

## ٢ · تعهّدات السلامة (ما لم يتغيّر)

- **الحسابات دون مساس:** `computeVariance`/`summarize` بلا تغيير. البلاطات العلويّة تبقى على الإجمالي، والفلاتر تضيّق الجدول فقط (سلوكٌ مطابقٌ للسابق).
- **مطابقة الحالات نفسها حرفيًّا:** مسمّيات الحالات ودلالاتها كما هي (`repStatusMatch` يطبّق العجز/الزيادة/… بنفس الشروط) — التعدّد اتّحادٌ لها فقط.
- **الطباعة والتصدير متّسقان تلقائيًّا:** يقرآن الاختيارين عبر `filteredReportRows` نفسها؛ «مراجعة الفرع» وExcel يحترمان الفئات والحالات المختارة بلا تغييرٍ في تنسيقهما.
- **لا مساس بمسار العدّ ولا الماسح ولا الصلاحيات** — التغيير محصورٌ في شاشة التقارير.

---

## ٣ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.source.html` · `index.html` | مكوّن `mf` المنبثق المتعدّد (فلترا الفئات/الحالات)، فلتر حالاتٍ اتّحاديّ (`repStatusMatch`)، بلاطة «من الكتالوج» وفلترها، وCSS `.mfilt` (يعيد استخدام `.msel`) |
| `build_harness.js` | خطّاف `window.__mf(box)` الموحّد للفلترين + البلاطات |
| `test_catfilter.mjs` | ٢٦ فحصًا (ط الفئات · ح الحالات · ك بلاطة الكتالوج) |
| `test_notes_cache.mjs` · `test_varamounts.mjs` · `test_select_ux.mjs` | تحديث ضبط فلتر الحالات إلى الخطّاف الجديد |
| `run_all_tests.mjs` · `README.md` · `.github/workflows/ci.yml` | تحديث العدّ (١٬٥٠٧ ← ١٬٥١٦) |
| `تعليمات-تركيب-فلترة-منبثقة-وبلاطة-كتالوج.md` | هذه الوثيقة |

---

## ٤ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp`.
3. `node build_release.js` — «الشيفرة متطابقةٌ حرفًا بحرف ✓».
4. `node build_harness.js` ثمّ `node run_all_tests.mjs` — **`TOTAL: 1516/1516 — ALL GREEN ✓`**.
5. ادفع إلى `main`.

**لا تغيير في أيّ حساب ولا قاعدة ولا صلاحيّة.**
