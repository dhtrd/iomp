# معالجة خطأ «resource-exhausted» — نفاد حصّة Firebase اليوميّة

**الحزمة:** `iomp-معالجة-نفاد-الحصة.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1490/1490 — ALL GREEN ✓` (٥٤ ملفًّا · +٥ فحوص) · **`firestore.rules` بلا تغيير**

---

## ١ · ما الذي حدث فعلًا؟

الخطأ في الصورة **ليس انقطاع إنترنت** — أنت متّصل. الرمز `resource-exhausted` معناه أنّ مشروع Firebase **بلغ حصّته اليوميّة المجّانيّة** على خطّة Spark، فرفض Firestore الكتابة.

حصص خطّة Spark المجّانيّة (من توثيق Firebase الرسميّ):

| المورد | الحدّ اليوميّ المجّانيّ |
|---|---|
| الكتابات | **٢٠٬٠٠٠ / يوم** |
| القراءات | **٥٠٬٠٠٠ / يوم** |
| الحذف | ٢٠٬٠٠٠ / يوم |
| التخزين | ١ غيغابايت |

**تتجدّد الحصّة تلقائيًّا نحو منتصف الليل بتوقيت المحيط الهادئ — أي نحو ١٠ صباحًا بتوقيت السعوديّة.** كلّ عدٍّ يمسّ عدّة كتابات، وكلّ فتح شاشةٍ يستهلك قراءات؛ فالجرد الكبير أو تعدّد العدّادين يبلغ الحدّ سريعًا.

---

## ٢ · ماذا تفعل الآن (تشغيليًّا)

ثلاثة حلول — بترتيب الأثر:

1. **ترقية الخطّة إلى Blaze (الحلّ الجذريّ للجرد الكبير):** خطّة «الدفع حسب الاستهلاك» ترفع الحدّ اليوميّ فورًا. **الطبقة المجّانيّة تبقى مشمولة**، وما يتجاوزها زهيدٌ جدًّا (كلّ ١٠٠ ألف كتابةٍ إضافيّة بسنتاتٍ قليلة). لأعمالٍ تجري جردًا كبيرًا هذا هو الحلّ الصحيح. تُفعَّل من: Firebase Console ← ⚙ ← Usage and billing ← Modify plan.
2. **الانتظار حتّى التجدّد (نحو ١٠ صباحًا بتوقيت السعوديّة):** تعود الكتابة تلقائيًّا بلا أيّ إجراء.
3. **تفعيل «العدّ دون اتصال»:** كي لا تُفقد العدّات أثناء نفاد الحصّة — تُحفظ محلّيًّا وتُزامَن تلقائيًّا حين تتجدّد الحصّة. (Firebase Console لا يلزم — إعدادٌ داخل التطبيق: صلاحيّات/ميزات ← «العدّ دون اتصال».)

---

## ٣ · ما أصلحته في التطبيق (مهمّ)

كان في التطبيق **خللٌ يُضاعف الضرر**: خطأ `resource-exhausted` لم يكن مُصنَّفًا خطأً «مؤقّتًا»، فكان العدّ **يُفقد** (رسالة فشلٍ فقط، بلا حفظٍ في الطابور). أصلحتُ ذلك:

- **العدّ لم يعد يُفقد عند نفاد الحصّة:** صار `resource-exhausted` يُعامَل خطأً مؤقّتًا يُدرَج في **طابور الأوفلاين** ويُعاد تلقائيًّا (لا يُعزَل رفضًا دائمًا) — فحين تتجدّد الحصّة (أو تُرقّى الخطّة) تُزامَن العدّات وحدها. *(شريطة تفعيل «العدّ دون اتصال» — لذا فعّله.)*
- **رسالةٌ واضحةٌ بدل الرمز الغامض:** بدل «resource-exhausted» تظهر: «بلغتَ حصّة Firebase اليوميّة المجّانيّة… تتجدّد نحو منتصف الليل بتوقيت المحيط الهادئ. فعّل العدّ دون اتصال… أو رقِّ الخطّة إلى Blaze».

*مثبَّتٌ بالفحوص O11 في `test_offline_live`:* الحصّة تُصنَّف مؤقّتة (تُعاد لا تُعزَل)، بخلاف `permission-denied` الذي يبقى رفضًا دائمًا؛ والعدّ يدخل الطابور فلا يُفقد؛ والرسالة عربيّةٌ واضحة.

**حدُّ الإصلاح بصدق:** هذا يمنع **فقدان** العدّات ويشرح السبب، لكنّه **لا يُنشئ حصّة** — لن تُكتب العدّات في السحابة فعلًا حتّى تتجدّد الحصّة أو تُرقّى الخطّة. وهو يعمل حين يكون **«العدّ دون اتصال» مفعّلًا**؛ لذا تفعيله ضروريّ لبيئة الجرد.

---

## ٤ · توصية

للأعمال التي تجري جردًا يوميًّا كبيرًا أو بعدّة عدّادين: **رقِّ إلى Blaze** (التكلفة زهيدة والحدّ يرتفع)، و**فعّل «العدّ دون اتصال»** دائمًا (شبكة أمانٍ توافق فلسفة «الأوفلاين أوّلًا»). وإن أردت، أضيف **تحذيرًا استباقيًّا** يظهر حين تقترب من الحدّ (مثلًا عند ٨٠٪) قبل أن ينفد — قُل، أنفّذه.

---

## ٥ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.html` · `index.source.html` | `offlineIsConnErr` يشمل `resource-exhausted` + رسالة `errAr` واضحة |
| `build_harness.js` | خطّافا `__offline.classify` و`__offline.errAr` |
| `test_offline_live.mjs` | +٥ فحوص (O11) — ٤٣ |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدد (١٤٨٥ ← ١٤٩٠) |
| `تعليمات-معالجة-نفاد-حصة-Firebase.md` | هذه الوثيقة |

---

## ٦ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp`.
3. `node build_harness.js` ثمّ `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1490/1490 — ALL GREEN ✓`**.
4. ادفع إلى `main`.

**لا تغيير في أيّ حساب ولا قاعدة ولا صلاحيّة — الإصلاح في تصنيف الخطأ ورسالته فقط.**
