# تسليم — إصلاح تداخل الإدخال بين العدّادين + فصل سجلّ النشاط

**الحزمة:** `iomp-إصلاح-التداخل-وفصل-السجلّ.zip` · **التاريخ:** ٢٦ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1483/1483 — ALL GREEN ✓` (٥٤ ملفًّا · +٩ فحوص) · **`firestore.rules` بلا تغيير**

> مشكلتان اثنتان من الميدان، مُصلحتان معًا. **بلا تغيير قاعدة عملٍ أو حساب أو تقرير أو صلاحيّة، وبلا مساسٍ بالقواعد.**

---

## ١ · المشكلة الأولى — تداخل الإدخال اليدويّ بين عدّادين

**العرَض الذي وصفتَه:** حين يسجّل عدّادٌ كميّةً، يعجز الآخر عن الكتابة يدويًّا في خانة الكمية لبضع ثوانٍ حتّى يتوقّف الأوّل.

**السبب الجذريّ (مؤكَّد بالشيفرة):** كتابة العدّاد الأوّل تصل جهاز الثاني عبر مستمع العدّات (`onSnapshot`)، فيُطلق **إعادة رسمٍ كاملة** لقائمة العدّ (`renderCountList` → `innerHTML=`). وإعادة البناء الكاملة كانت **تمحو حقل «أضف» الذي يكتب فيه الثاني وتسرق تركيزه** — فما دام الأوّل يكتب بتتابع، تتكرّر إعادة الرسم على جهاز الثاني فيتعذّر عليه الإدخال. (الترقيع الموضعيّ لصفٍّ واحد كان يحفظ الحقل، لكنّ إعادة البناء الكاملة لم تكن تحفظه.)

**الإصلاح:** قبل إعادة البناء الكاملة، تُحفظ حالة الحقل المركَّز (خانة «أضف» أو محرّر الملاحظة): رمزُ الصنف والقيمة وموضع المؤشّر؛ وبعد البناء تُستعاد. فلم تعد تحديثات العدّاد الآخر **تمحو ما يكتبه هذا العدّاد ولا تسرق تركيزه** — يكتب بسلاسةٍ ولو انهمرت تحديثات زميله.

*مثبَّتٌ بالفحوص ص١–ص٣:* بعد إعادة رسمٍ كاملة (مرّةً ومرّتين متتاليتين) يبقى التركيز في خانة «أضف» وقيمتها «٧» سليمة.

**أثر جانبيّ؟** لا — الإصلاح لا يغيّر أيّ حساب؛ يحفظ تجربة الإدخال فقط. صفر Firestore إضافيّ.

---

## ٢ · المشكلة الثانية — سجلّ نشاطٍ لكلّ عدّاد، والمدير يرى الاثنين

**طلبك:** كلّ عدّادٍ يرى سجلّه هو فقط؛ والمدير **غير الموكَّل بالجرد** يرى سجلّي العدّادين معًا.

**الوضع قبل:** سجلّ النشاط كان يعرض حركات **الجميع مختلطةً** لكلّ من يفتح الجلسة.

**الإصلاح (بحسب المُلكيّة لا الدور):**

- **العادّ الموكَّل بالجرد** (اسمه في `assignedCounters`) يرى **سجلّه هو فقط** — تُفلتَر الحركات على `by === معرّفه`. ولافتةٌ تقول «🧾 سجلّ نشاطك أنت — كلّ عدّادٍ يرى سجلّ عدّه فقط».
- **المدير/المطّلع غير الموكَّل** يرى **الجميع**، وله **أزرار فلترةٍ بالعدّاد** (الكلّ · فلانٌ · فلان) ليطالع سجلّ كلّ عدّادٍ على حدة أو مجتمعين.
- **صفر تغييرٍ في القواعد ولا فهارس:** الفلترة في الواجهة على حركاتٍ تحمل `by` أصلًا (يكتبه `logActivityRaw`). لا قراءةٌ إضافيّة ولا مستند جديد.

*مثبَّتٌ بالفحوص فص١–فص٦:* الموكَّل يرى صنفه دون الآخر ودون اسمه؛ وغير الموكَّل يرى الاثنين مع أزرار الفلترة بالاسمين؛ والفلترة بعدّادٍ تعرض سجلّه وحده.

**ملاحظة صدقٍ على العمق:** السجلّ يُحمَّل بحدّ ٤٠ حركةً حديثة، والفلترة في الواجهة. في الحالة النادرة (عدّادٌ آخر يُغرق الحدّ بحركاتٍ كثيرةٍ جدًّا في لحظةٍ واحدة) قد يرى العادّ عددًا أقلّ من حركاته الحديثة. إن أردت ضمان عمقٍ ثابتٍ لكلّ عدّاد، أرفع الحدّ أو أضيف فهرسًا مخصّصًا — قرارٌ صغيرٌ أعرضه إن احتجتَه.

---

## ٣ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.html` · `index.source.html` | حفظ حقل الإدخال في `renderCountList` + فصل `renderActivity` بحسب العدّاد |
| `build_harness.js` | خطّافات فحص: `__rcl` و`__act.render/setAssigned/clickBy/byFilter` |
| `test_scanflow.mjs` | +٣ فحوص (المجموعة «ص») — ١١٥ |
| `test_history_runs.mjs` | +٦ فحوص (المجموعة «فص») — ١٣٤ |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدد (١٤٧٤ ← ١٤٨٣) |
| `تعليمات-إصلاح-التداخل-وفصل-السجلّ.md` | هذه الوثيقة |

---

## ٤ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp` (استبدالٌ مباشر).
3. `node build_harness.js` ثمّ `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1483/1483 — ALL GREEN ✓`**.
4. ادفع إلى `main` ودع CI يحكم.

---

## ٥ · ما لم يتغيّر

لا قاعدة عملٍ، ولا حساب، ولا تقرير، ولا صلاحيّة. `computeVariance` والعدّ والمزامنة بلا مساس. `firestore.rules` بلا تغيير. الإصلاحان يمسّان **تجربة الإدخال وعرض السجلّ** فقط، وصفر أثرٍ سحابيّ.
