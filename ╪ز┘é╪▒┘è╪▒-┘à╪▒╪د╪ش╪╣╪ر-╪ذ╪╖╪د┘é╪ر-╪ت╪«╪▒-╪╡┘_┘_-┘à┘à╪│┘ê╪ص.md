# تقرير مراجعة تجربة المستخدم — بطاقة «آخر صنف مُمسوح» (تحسينٌ مقترح، بلا تنفيذ)

**التاريخ:** ٢٦ يوليو ٢٠٢٦ · **العنصر:** دالّة `scanPanel` → العنصر `#scanStatus` (سطر ٤٩٨٩)
**الحالة:** تحليلٌ ومخرجاتٌ لِما قبل التنفيذ — **بانتظار موافقتك قبل كتابة أيّ شيفرة.**

---

## ٠ · تمييزٌ لا بدّ منه: هذه بطاقةٌ غير نافذة م٧

كي لا نخلط بين عنصرين متشابهين:

| | **بطاقة «آخر صنف مُمسوح»** (هذا الطلب) | **نافذة اعتماد الكتالوج م٧** (نُفِّذت) |
|---|---|---|
| المعرّف | `#scanStatus` (`scanPanel`) | `#caOverlay` (`caShell`) |
| متى تظهر | بعد مسح صنفٍ **موجودٍ في الجلسة** | عند مسح صنفٍ في **الكتالوج الرئيسيّ لا في الجلسة** |
| طبيعتها | بطاقةٌ سطريّةٌ أسفل حقل المسح | نافذةٌ مركزيّة تحتاج قرارًا (أضِف/أغلق) |
| الكتابة | المسح **يكتب فورًا** (عدٌّ ملتزَم) | لا يُكتب شيءٌ قبل الاعتماد |

طلبك: اجعل **البطاقة السطريّة** معاينةً حيّةً دائمة — وهي **الفلسفة نفسها** التي طبّقناها في م٧ (لا تختفي تلقائيًّا · تُستبدَل عند المسح التالي · إغلاقٌ بـ×· صفر كلفةٍ سحابيّة · تحديثٌ موضعيّ). أي أنّنا نبني على نمطٍ **مثبَّتٍ ومُختبَر** لا على مجهول.

---

## ١ · السلوك الحاليّ للبطاقة (بالشيفرة)

`scanPanel(o)` (سطر ٤٩٨٩) يرسم في `#scanStatus`. بعد مسحٍ ناجحٍ لصنفٍ في الجلسة (سطر ٥٠٤٨) تعرض البطاقة:

| الحقل الحاليّ | المصدر |
|---|---|
| اسم المنتج (العنوان) | `it.name` |
| الباركود | `job.code` |
| الكمية الحالية (قبل) | `before` |
| الكمية المضافة | `+1` (أخضر) |
| الإجمالي بعد التحديث | `after` (بارز) |
| الحالة | «تمّ العدّ ✓» / «جارٍ التسجيل…» / «لم تُسجَّل» |

**وأهمّ ما فيها — الاختفاء التلقائيّ (سطر ٥٠٠١):**

```js
if(st!=='pending') el._t=setTimeout(()=>{ … display='none' … }, st==='ok'?3200:6000);
```

فبعد مسحةٍ ناجحة تختفي البطاقة خلال **٣٫٢ ثانية**، وبعد خطأٍ خلال **٦ ثوانٍ**. أثناء المسح السريع تبقى (كلّ مسحةٍ تُصفّر المؤقّت)، لكن **بمجرّد أن يتوقّف العدّاد لحظةً، تختفي**.

**الأداء الحاليّ (نقطةٌ لصالح النظام):** البطاقة تقرأ `curCounts`/`it` المحلّيّة فقط — **صفر قراءةٍ/كتابةٍ سحابيّة** — وتحدّث `innerHTML` لعنصرٍ صغيرٍ واحد (ليست إعادة رسمٍ كاملة؛ `renderCountList` منفصلةٌ ومؤجَّلة). فالبنية الأدائيّة **جاهزةٌ** لِما تطلبه.

---

## ٢ · لماذا قد يُربك السلوك الحاليّ المُجرِد

خمس نقاطٍ رصدتُها بعين المُجرِد والمشرف:

**١) تختفي بعد ٣٫٢ ثانية.** في جردٍ ميدانيٍّ حقيقيّ، العادّ يمسح ثمّ ينحني للرفّ التالي أو ينقل بصره؛ فيعود إلى **بطاقةٍ فارغة** وقد فقد تأكيد ما عدّه للتوّ. ثلاث ثوانٍ قصيرةٌ على إيقاع العمل الفعليّ.

**٢) لا تعرض الرصيد الدفتريّ ولا الفرق ولا حالة الجرد.** يرى العادّ «الإجمالي ١٣» لكن لا يرى «الدفتري ١٠، الفرق +٣، زيادة». فلا يستطيع أن يلتقط خطأً لحظيًّا — كأن يمسح رفًّا خطأً فيقفز الفرق فجأةً. المعلومة التي تكشف الخطأ **غائبةٌ عن عينه**.

**٣) لا زرّ إغلاق.** لا يملك العادّ أن يُغلقها حين ينتهي، ولا أن يُبقيها ظاهرةً حين يريد؛ هو رهنٌ للمؤقّت وحده.

**٤) سلوكٌ غير متّسق.** «تمّ العدّ» يختفي بعد ٣٫٢ث و«لم تُسجَّل» بعد ٦ث — توقيتان مختلفان يجعلان ظهور البطاقة يبدو عشوائيًّا.

**٥) الفرق بلا ترميزٍ لونيّ.** لا إشارة بصريّة فوريّة تميّز الزيادة من العجز من المطابقة.

**الخلاصة:** البطاقة اليوم **إيصالٌ عابر**، والمطلوب أن تصير **لوحة معاينةٍ حيّة** ثابتةً تُطمئن العادّ وتكشف الخطأ فورًا.

---

## ٣ · السلوك الجديد المقترح (مطابقٌ لطلبك)

**الثبات:** لا تختفي تلقائيًّا أبدًا. تبقى حتّى:

1. يضغط العادّ **(×)** فتُغلق.
2. يمسح صنفًا آخر ⇒ **لا تُغلق**، بل **تُستبدَل محتوياتها فورًا** بالصنف الجديد — بلا تلاشٍ، بلا حالةٍ فارغة، استبدالٌ لحظيّ.

لا شيء آخر يُخفيها.

**الحقول الثمانية (كلّها دائمًا):**

| الحقل | المصدر (محلّيّ) |
|---|---|
| اسم المنتج | `it.name` |
| الباركود | `it.code` |
| **الرصيد الدفتريّ** | `it.book` |
| الكمية المضافة بهذا المسح | `+1` |
| **الكمية الفعلية الحالية** | `after` = `curCounts[code].qty` |
| **الفرق** (حيّ) | `after − (it.book‖0)` |
| **حالة الجرد** | زيادة / عجز / مطابق (من إشارة الفرق) |
| حالة المسح | تمّ العدّ ✓ / جارٍ التسجيل… / لم تُسجَّل |

**قواعد الكمية (حيّةٌ دائمًا):** دفتري ١٠ · فعلي ١٣ ⇒ فرق **+٣**. دفتري ٨ · فعلي ٥ ⇒ فرق **−٣**. يُحسب لحظيًّا مع كلّ مسحة (١٣→١٤ ⇒ +٣→+٤).

**الأولويّة البصريّة (كطلبك):** الرصيد الدفتريّ ↓ الكمية الفعلية ↓ **الفرق** — بتمييزٍ متدرّج، و**الفرق أقوى تمييزًا**؛ وثلاثة أنماطٍ متمايزة: **موجب** (أخضر تصاعديّ) · **سالب** (أحمر) · **صفر** (محايد/مطابق).

---

## ٤ · مبرّر تجربة المستخدم (UX Justification)

- **الثبات يمنع فقد التأكيد:** العادّ يرفع بصره ويعود فيجد آخر ما عدّه ما زال أمامه — لا «بطاقةٌ تبخّرت».
- **الفرق الحيّ المُبرَز يكشف الخطأ في لحظته:** قفزةٌ غير متوقّعةٍ في الفرق (+٣ ⇒ +١٢) تُنبّه العادّ أنّه مسح صنفًا/رفًّا خطأً قبل أن يمضي.
- **حالة الجرد تعطي المعنى لا الرقم فقط:** «زيادة/عجز» أوضح للعادّ من رقمٍ مجرّد.
- **الاستبدال الفوريّ يخدم المسح المتتابع:** لا وميض، لا حالةٌ فارغة بين صنفين — إيقاعٌ سلس.
- **زرّ الإغلاق يمنح العادّ السيطرة:** يُغلق حين يشاء، ويُبقي حين يشاء.
- **الاتّساق يبني الثقة:** سلوكٌ واحدٌ لا مؤقّتان مختلفان.

**وهو منسجمٌ مع م٧:** العادّ يتعلّم نمطًا واحدًا للبطاقتين (ثابتة · تُستبدَل · ×· ألوان الفرق) فيقلّ العبء الذهنيّ.

---

## ٥ · ملاحظة صحّةٍ حرِجة: الجلسة العمياء

في الجلسة العمياء (`curSess.blind`) يُخفى الرصيد الدفتريّ في قائمة العدّ اليوم (سطر ٣٣٢٧ `showBook=(!blind&&…)`). فالبطاقة الجديدة **يجب أن تُخفي الرصيد الدفتريّ والفرق وحالة الجرد في الجلسة العمياء** (لأنّها تُشتقّ من الدفتريّ)، وتكتفي بـ: الاسم · الباركود · المضاف · الفعلي · حالة المسح. وإلّا سرّبت البطاقة ما تُخفيه الجلسة العمياء — وهذا يُبطل الغرض من العمى. سأثبّت هذا بفحصٍ صريحٍ عند التنفيذ.

---

## ٦ · تحليل أثر الأداء (Performance Impact Analysis)

طلبك: بلا قراءات/كتابات Firestore إضافيّة، بلا إعادة رسمٍ كاملة، بلا تأخّرٍ محسوس، من الحالة المحلّيّة وحدها. وهذا **مضمونٌ بنيويًّا** لأنّ:

- **صفر Firestore:** البطاقة تقرأ `it` (اللقطة المحلّيّة) و`curCounts[code].qty` (المحلّيّة) فقط. المسح نفسه يكتب (سلوكٌ قائمٌ لا إضافيّ)؛ البطاقة تضيف **صفر** قراءةٍ وصفر كتابة.
- **لا إعادة رسمٍ كاملة:** البطاقة عنصرٌ واحدٌ (`#scanStatus`) منفصلٌ عن `renderCountList` (المؤجَّلة أصلًا خارج المسار الحرج). تحديثها لا يمسّ قائمة العدّ.
- **تحديثٌ موضعيّ عند إعادة مسح الصنف نفسه:** كنمط م٧ (`caTouch`)، نحدّث **عقدتين نصّيّتين** (الفعلي والفرق) بدل إعادة بناء البطاقة — قِسنا في م٧ فرقًا ≈١٩ ضعفًا (٠٫٠٨٥ مقابل ٢٫٥ مللي). إعادة البناء الكاملة تقع فقط عند **تبديل المنتج**.
- **إزالة المؤقّت توفّرٌ لا كلفة:** حذف `setTimeout` الاختفاء يزيل عملًا، لا يضيفه.

سأقيس بمسبارٍ مخصّص (كـ`probe_ca_render`) قبل/بعد لأثبت الأرقام لا أدّعيها.

---

## ٧ · بنيةٌ مقترحة (للموافقة) وبديلٌ معروض

**المقترح (الموصى به):** تعميم نمط م٧ على `scanPanel`: حالةٌ محفوظةٌ لآخر صنفٍ معروض (`code`,`name`,`book`,`after`)، رسمٌ كاملٌ عند تبديل المنتج وتحديثٌ موضعيٌّ عند تكراره، زرّ ×، وإزالة الاختفاء التلقائيّ. **بلا تغيير قاعدة عملٍ ولا حساب** — البطاقة مرآةٌ للعدّ الملتزَم لا مصدره.

**بديلٌ أعرضه ولا أفرضه:** **توحيد البطاقتين** (`scanPanel` و م٧) في مكوّنٍ بصريٍّ واحد. *ميزته:* اتّساقٌ تامّ. *عيبه:* م٧ نافذةٌ قراريّة (أضِف/أغلق دائم) والبطاقة إعلاميّة (عدٌّ ملتزَم)؛ دمجهما يخلط دلالتين ويخاطر بفحوصٍ مستقرّة. **توصيتي: الإبقاء على عنصرين متمايزين بصريًّا متناسقين** — لا دمجهما. القرار لك.

---

## ٨ · ما لن يتغيّر (التزامٌ ثابت)

لا قاعدة عملٍ، ولا معادلة، ولا تقرير، ولا صلاحيّة. البطاقة تعرض ما هو محسوبٌ أصلًا. الماسح يبقى جاهزًا (حارس التركيز)، والمعرّفات وRTL والطباعة بلا مساس، وصفر أثرٍ على أداء المسح.

---

## ٩ · المطلوب منك

راجِع هذا التقرير مع **النموذج البصريّ** و**مخطّط التدفّق** المرفقين. عند موافقتك أُنفّذ، وأُسلّم الحزمة بالفحوص والقياسات كعادتنا. وإن رأيت تعديلًا في الحقول أو الأولويّة البصريّة أو سلوك الإغلاق — قُله الآن قبل التنفيذ.
