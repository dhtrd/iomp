# تسليم — إظهار «من أضاف» (تفاصيل العدّات) في طباعة «مراجعة الفرع»

**الحزمة:** `iomp-من-أضاف-في-طباعة-الفرع.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1485/1485 — ALL GREEN ✓` (٥٤ ملفًّا · +٢ فحص) · **`firestore.rules` بلا تغيير**

> إضافةُ عمودٍ إلى **طباعة** مستند «مراجعة الفرع». **لا تغيير في أيّ حساب ولا في كميّةٍ ولا فرقٍ ولا مجاميع.**

---

## ١ · المشكلة

عند طباعة مستند **«مراجعة الفرع»** لم يكن عمود **«من أضاف» (تفاصيل العدّات)** يظهر.

**السبب (بالشيفرة):** نظام طباعة المستندات (`buildPrintReport` + `PRINT_COLDEF`) **لم يكن يملك عمود «من أضاف» أصلًا** — كان موجودًا في تقرير الشاشة وتصدير Excel فقط. وسبب «مراجعة الفرع» أعمدتُه: باركود، اسم، فئة، دفتري، فعلي، فرق — بلا «من أضاف».

---

## ٢ · الإصلاح

- عرّفتُ عمود **«من أضاف»** في تعريفات أعمدة الطباعة (`PRINT_COLDEF`) — يعرض تفاصيل العدّات لكلّ عدّاد بالصيغة نفسها المعتمَدة في الشاشة والتصدير (`whoDetail`).
- أضفتُه إلى أعمدة سبب **«مراجعة الفرع»**، وأعطيته عرضًا أوسع في توزيع الأعمدة (لاحتواء أسماء العدّادين).
- حدّثتُ تلميح الزرّ ليذكر «من أضاف».

**النتيجة في الطباعة** (متّسقةٌ مع إصلاح «من أضاف» السابق):

| الحالة | يظهر في الطباعة |
|---|---|
| تتابعُ مسحٍ ممسوح | `عبدالكريم الضيفي: 3` (مجموعٌ بلا ١+١) |
| إضافتان منفصلتان حقيقيّتان | `عبدالكريم الضيفي: 6 (4 + 2)` (مفصّلة) |

**ملاحظةٌ على التنسيق:** صار المستند الآن ثمانية أعمدة (٦ + «من أضاف» + الملاحظات التلقائيّة إن وُجدت)، فيُصغّر نظامُ «احتواء الطباعة» البنطَ تلقائيًّا (١٠٫٨px بدل ١١٫٥) ليتّسع كلّ شيءٍ في الورقة — سلوكٌ قائمٌ يعمل من نفسه. وإن أردتَ فسحةً أوسع فطباعةٌ أفقيّة (من إعدادات المخرجات) تريّحه.

---

## ٣ · لماذا هذا آمنٌ

- **صفر تغييرٍ في حساب:** عمودٌ عرضيٌّ فقط، مصدرُه `r.who` المحسوب أصلًا. الكميّات والفروقات والمجاميع بلا مساس.
- **يُخفى تلقائيًّا حين لا عدّات** («—») ولا يكسر التخطيط.
- `firestore.rules` والمعرّفات وRTL بلا مساس.

*مثبَّتٌ بالفحوص:* `test_printfit` P2 (ثمانية أعمدة + «من أضاف») وP9 (التفاصيل تظهر: تتابعٌ `3` بلا ١+١، ومنفصلةٌ `6 (4 + 2)`).

---

## ٤ · نطاقٌ مقصود

أضفتُ العمود إلى **طباعة «مراجعة الفرع»** كما طلبتَ. مستند «تقرير مفصّل» أعمدتُه قابلةٌ للتهيئة من الإعدادات، وبقيّة المستندات لها أغراضها. إن أردتَ «من أضاف» في مستندٍ آخر (مثلًا التقرير المفصّل أو تصدير Excel لمراجعة الفرع) فقُل، أُضِفه بالطريقة نفسها.

---

## ٥ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.html` · `index.source.html` | عمود `who` في `PRINT_COLDEF`/`PRINT_COLW` + إضافته لسبب «مراجعة الفرع» + تلميح الزرّ |
| `test_printfit.mjs` | تحديث P2 + فحص P9 جديد (١٥ بدل ١٣) |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدد (١٤٨٣ ← ١٤٨٥) |
| `تعليمات-إظهار-من-أضاف-في-طباعة-الفرع.md` | هذه الوثيقة |

---

## ٦ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp`.
3. `node build_harness.js` ثمّ `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1485/1485 — ALL GREEN ✓`**.
4. ادفع إلى `main`.

---

الخطوة التالية باختيارك المُسجَّل: **تنفيذ فلتر الفئات المتعدد بالنمط B**.
