# إصلاح: منبثق الفلترة لا يُغلق — ط-٢٠

**الحزمة:** `iomp-إصلاح-إغلاق-المنبثق.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1523/1523 — ALL GREEN ✓` (+٢ فحص) · **`firestore.rules` بلا تغيير** · **حجم النشر ١٨٩٬٤١٧ بايت مضغوطًا**.

---

## ١ · المشكلة

في فلترَي «كلّ الفئات» و«كلّ الحالات» المنبثقين: كان المنبثق **لا يختفي** عند الضغط على الزرّ لإغلاقه، ولا عند فتح الفلتر الآخر — فيظهر الاثنان معًا.

## ٢ · السبب الجذريّ

قاعدة `‎.mfilt-pop{display:flex}` كانت **تتغلّب** على آليّة الإخفاء القياسيّة (سمة `hidden` التي تعني `display:none`). فحين يطلب الكود إغلاق المنبثق (بضبط `hidden`)، كان `display:flex` يُبقيه ظاهرًا. المنطق كان يعمل (الصنف `open` يُزال، والمستمعون يُغلقون)، لكنّ **العرض** بقي ظاهرًا بصريًّا فقط.

## ٣ · الإصلاح

قصْر قاعدة الـflex على الحالة **غير المخفيّة** فقط:

```css
.mfilt-pop:not([hidden]){display:flex;flex-direction:column;gap:6px}
```

الآن حين يُضبط `hidden` يسري `display:none` فيُغلق المنبثق فعليًّا — بالضغط على الزرّ (toggle)، أو بفتح الفلتر الآخر، أو بالنقر خارجه، أو بـEsc. سلوكٌ صحيحٌ لقائمةٍ منبثقةٍ منسدلة.

**تقويةُ الاختبار:** أُضيف خطّاف `popShown()` يقيس **الظهور الفعليّ** (لا مجرّد صنف `open`)، وثلاثةُ فحوصٍ تتحقّق أنّ المنبثق يختفي فعلًا عند الإغلاق وأنّ فتح أحدهما يُخفي الآخر — كي لا يتكرّر الانحدار.

---

## ٤ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.source.html` · `index.html` | قاعدة CSS واحدة: `‎.mfilt-pop:not([hidden])` |
| `build_harness.js` | خطّاف `popShown()` (الظهور الفعليّ) |
| `test_catfilter.mjs` | تقوية ط١٥ + فحصان (toggle · إخفاء الآخر) — ٢٨ |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدّ (١٬٥٢١ ← ١٬٥٢٣) |

---

## ٥ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع.
3. `node build_release.js` ← `node build_harness.js` ← `node run_all_tests.mjs` = **`1523/1523 — ALL GREEN ✓`**.
4. ادفع إلى `main`.

**لا تغيير في أيّ حساب ولا قاعدة ولا صلاحيّة — إصلاح CSS فقط.**
