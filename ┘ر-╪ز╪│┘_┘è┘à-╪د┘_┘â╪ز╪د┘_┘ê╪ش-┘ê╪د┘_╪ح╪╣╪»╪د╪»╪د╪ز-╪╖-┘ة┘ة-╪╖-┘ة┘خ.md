# تقرير التسليم الختاميّ — الكتالوج الرئيسيّ ومعمارية جلسة الجرد الذكيّة

**التاريخ:** ٢٦ يوليو ٢٠٢٦ · **المهامّ:** ط-١١ … ط-١٦ · **الحالة:** ✅ منجَزٌ وأخضر
**الاختبارات:** `TOTAL: 1391/1391 — ALL GREEN ✓` (٥٤ ملفًّا) — **مرّتين**: على النسخة الموثّقة وعلى نسخة النشر
**`firestore.rules`:** لم يُمَسّ · **الصلاحيّات:** لم تُمَسّ · **الحسابات والتقارير:** لم تُمَسّ

---

## ١ · أين نحن من التكليف

التكليف ذو الإحدى عشرة مرحلة اكتمل. المراحل من الأولى إلى الحادية عشرة سُلّمت وثائقَ تصميمٍ واعتمدتَ بنودها السبعة كلّها («أعتمد البنود السبعة كلّها»)، ثمّ نُفّذت على ستّ مهامّ متتابعة:

| المهمّة | المحتوى | الحالة |
|---|---|---|
| ط-١٠ | طبقة الكتالوج (القارئ): `catalogChunks` + مرآة IndexedDB + بحث O(1) | ✅ سُلّمت سابقًا |
| **ط-١١** | استيراد ملفّ المنتجات: قارئ ملفّات مشترَك، مطابقة، تحديث، إدراج | ✅ |
| **ط-١٢** | تقرير الاستيراد | ✅ |
| **ط-١٣** | توصيل المسح بالكتالوج — **التدفّقان ٤ و٥** | ✅ |
| **ط-١٤** | إعادة تصميم سجلّ المسح — **المرحلة ٤** | ✅ |
| **ط-١٥** | الكفاءة والحصّة والقياس — **المرحلة ٦** | ✅ |
| **ط-١٦** | لوحة الإعدادات — **٤٣ مقبضًا** | ✅ |

---

## ٢ · التدفّقات الخمسة كما طُلبت حرفًا بحرف

### التدفّق ١ — الكتالوج الرئيسيّ

`استيراد ملفّ المنتجات ← مقارنة القائم ← تحديث التكلفة ← تحديث التصنيف ← تحديث الاسم ← إدراج الجديد ← توليد تقرير الاستيراد.`

الأعمدة الإلزاميّة في الملفّ هي **نفسها** حقول «إضافة صنف يدويًّا» الإلزاميّة — تنفيذًا لنصّك: «في الرفع هو نفس حقول اضافة صنف يدوي الإلزامية والاختيارية»:

- **إلزاميّ:** الباركود · الاسم · التصنيف · التكلفة
- **اختياريّ:** الوحدة · الرصيد الدفتريّ

قارئ الملفّات **مشترَكٌ** مع رفع لقطة الجلسة — لم أكتب قارئًا ثانيًا، لأنّ قارئين يعنيان اختلافًا في التسامح مع الترميز والفواصل والصيغ، والاختلاف هنا خطأٌ صامتٌ في البيانات.

### التدفّق ٢ — إنشاء الجلسة

`استيراد لقطة الجرد ← إنشاء الجلسة ← تخزين اللقطة.` قائمٌ من قبل ولم يُمَسّ.

### التدفّق ٣ — مسح باركودٍ موجودٍ في الجلسة

`يزيد الكمية ← يحدّث المعاينة ← جاهزٌ للمسحة التالية.` قائمٌ من م٦-٥ ولم يُمَسّ.

### التدفّق ٤ — باركودٌ غير موجودٍ في الجلسة وموجودٌ في الكتالوج

يبحث في الكتالوج الرئيسيّ ← يعرض حوار الموافقة بنصّك حرفيًّا:

> «هذا الصنف غير موجود في جلسة الجرد هذه. وهو موجود في الكتالوج الرئيسيّ. هل تريد إضافته؟»

وعند الموافقة يُدرج في الجلسة الحاليّة بـ: **الرصيد الدفتريّ = ٠ · الكمية الفعليّة = ١ · التكلفة = تكلفة الكتالوج · التصنيف = تصنيف الكتالوج · الباركود = باركود الكتالوج**، ويصير **جاهزًا فورًا للمسحة التالية**.

وهذا الإدراج **لا يضيف سطر حسابٍ واحدًا**: كتابة `book:0` تسلك فرعًا **قائمًا** في `computeVariance` فيُصنَّف الصنف `surplus` تلقائيًّا — احترامًا لـ«لا تغيّر الحسابات».

### التدفّق ٥ — غير موجودٍ في أيّ مكان

تنبيه «الباركود غير موجود في الكتالوج الرئيسيّ» ثمّ **يواصل المسح فورًا** — بلا نقرة، بلا إغلاق حوار، بلا فقدان تركيز حقل المسح. ويُسجَّل الباركود المجهول في قائمةٍ **محلّيّة** (`unknown.logLocal`، سقف ٥٠٠) لتراجعها لاحقًا — بصفر كتاباتٍ في Firestore.

**الدليل:** `test_catalog_scanadd.mjs` — ١٠٦ تأكيدات.

---

## ٣ · المرحلة ٤ — سجلّ المسح أُعيد تصميمه

السلوك القديم كان غير مقبولٍ كما قلت. الجديد:

- المسحات **المتتابعة** للصنف نفسه تُدمج في سطرٍ واحد: `أحمد ×٣`.
- وإن تخلّلها صنفٌ آخر (أ، أ، أ، ب، أ) صارت: `أحمد ×٣` ثمّ `ب ×١` ثمّ `أحمد ×١`.
- **كلّ سطرٍ يمثّل تتابعَ عدٍّ واحدًا متّصلًا. ولا تُدمج تتابعاتٌ مفصولة** — حرفيًّا كما طلبت.

والمقبض في يدك: `history.mergeRuns` في الإعدادات. إطفاؤه يعيد السطر لكلّ مسحة كما كان — **والكمية لا تتغيّر**. وهذا مُثبَتٌ بتأكيدٍ صريحٍ في `test_settings_panel.mjs` (المجموعة ح): ثلاث مسحاتٍ = ثلاثة أسطر عند الإطفاء، وسطرٌ واحدٌ عند التشغيل، **والكمية ٣ في الحالتين**. الرقم لا يتبع شكل السجلّ أبدًا.

**الدليل:** `test_history_runs.mjs` — ١٢٨ تأكيدًا.

---

## ٤ · المرحلة ٥ — الأوفلاين أوّلًا

سلسلة العدّ كما طلبتها: `الماسح ← الذاكرة ← IndexedDB ← الواجهة ← الطابور ← مزامنة الخلفيّة ← Firestore`.

والنقطة الحاسمة: **البحث وقت المسح لا يمسّ Firestore إطلاقًا.** مرآة الكتالوج في IndexedDB تخدم كلّ عمليّات البحث بصفر قراءات. تتحقّق منه بنفسك في `harness.html`:

```js
window.__cat.reads();   // يبقى صفرًا مهما بحثت
```

Firestore ليس قاعدة العدّ — هو للمزامنة والمشاركة والنسخ والتاريخ فقط، كما نصصت.

---

## ٥ · المرحلة ٦ — الكلفة، وقياسٌ لا تخمين

### الاكتشاف الحاكم

**Firestore يحاسب على كلّ مستندٍ يُسلَّم لكلّ مستمع.** كتابةٌ واحدة يراها خمسة أجهزة = **خمس قراءاتٍ محسوبة**. هذا يمثّل نحو ٩٠٪ من كلفة التطبيق، والنموذج:

```
القراءات/يوم = (S ÷ m) × (1 + N + N + R)
الكتابات/يوم = (S ÷ m) × 2
```

حيث `S` عدد المسحات و`m` عدد المسحات في الدفعة الواحدة و`N` عدد الأجهزة.

### ما فعلته

- **دفعاتٌ لا كتابةً لكلّ مسحة.** لا كتابة Firestore لكلّ باركود — طابورٌ محلّيّ ودفعات، كما نصصت.
- **مستمع الحركة يفتح عند الطلب** (`activity.lazyListen`) لا عند الإقلاع — فمن لا يفتح سجلّ الحركة لا يدفع ثمنه.
- **سجلّ الحركة يُجمَّع** في سطرٍ لكلّ تتابعٍ متّصل بدل سطرٍ لكلّ مسحة (`history.actFlushMs = 2500`).
- **عدّاد حصّةٍ داخل التطبيق** يعدّ القراءات والكتابات الفعليّة، بسقفَي Spark (٥٠٬٠٠٠ و٢٠٬٠٠٠) وعتبتَي تحذير (٧٠٪ و٩٠٪).
- **قياس `m` الحقيقيّ** أثناء التشغيل بدل افتراضه.

### النتيجة المقيسة

| القياس | القيمة |
|---|---|
| السقف قبل التحسين | **~٤٬٥٤٥ مسحة/يوم** |
| السقف عند `m=3` مع المستمع الكسول | **~٢٥٬٠٠٠ مسحة/يوم** |
| قراءات الإقلاع | تستقرّ عند **٦** |
| فتح جلسة | **+٧** |
| الانتقال إلى أيّ شاشة | **+١** |
| شاشة الإعدادات | **+١ وقراءةٌ لكلّ مستخدم** (بطاقة الاستثناءات القائمة) |

**وتحذيرٌ واجب:** عدّاد الحصّة **تقديريّ ومحلّيّ لكلّ جهاز**، والحصّة نفسها **مشتركة بين كلّ الأجهزة** وتُصفَّر منتصف ليل توقيت المحيط الهادئ. اللوحة تقول هذا بنصّها الصريح ولا تتظاهر بعلمٍ لا تملكه؛ وإن لم يقع قياسٌ بعد قالت **«لا قياسَ بعد»** بدل اختراع رقم.

**وعلى خطّة Spark لا فاتورة.** تجاوز الحصّة = **رفض العمليّات حتّى منتصف الليل**، لا مطالبةٌ ماليّة.

**الدليل:** `test_quota_lazy.mjs` — ٧٠ تأكيدًا.

---

## ٦ · المرحلتان ٧ و٨ — الموثوقيّة والدقّة

**عند فشل المزامنة:** يستمرّ العدّ، وتظهر «المزامنة معلّقة…»، وتُعاد المحاولة تلقائيًّا. **ولا يُقاطَع العامل أبدًا.**

**والدقّة لها الأولويّة المطلقة:** التقرير النهائيّ يُحسب **دائمًا من جلسة الجرد الحاليّة**، ولا يُحسب من سجلّ المسح ولا من الطابور ولا من سجلّ المزامنة. سجلّ المسح **للتدقيق فقط**. الجلسة هي مصدر الحقيقة الوحيد.

وأقوى دليلٍ على هذا هو اختبار المجموعة ح المذكور أعلاه: تغيير شكل السجلّ لا يحرّك الكمية بمقدار واحد.

---

## ٧ · المرحلة ٢ ← لوحة الإعدادات (ط-١٦): الوفاء بالنصّ

قلتَ: **«قم بتنفيذ الاصح مع امكانية تغيير ذلك من الاعدادات»**. فكلّ قرارٍ اتّخذته نيابةً عنك له **مقبضٌ ظاهرٌ في الواجهة** — لا مفتاحَ مدفونٌ في الشيفرة.

**٤٣ مفتاحًا في ثلاث بطاقات:**

| البطاقة | المفاتيح | أمثلة |
|---|---|---|
| **الكتالوج — الاستيراد والمطابقة** | ٢٥ | `import.maxRows=20000` · `cost.updatePolicy='always'` · `catalog.autoAdd='ask'` · `barcode.stripLeadingZeros=true` · `catalogAdd.bookZero=true` |
| **سجلّ المسح والحركة (المرحلة ٤)** | ٦ | `history.mergeRuns=true` · `history.actFlushMs=2500` |
| **الحصّة والكلفة والتخزين (المرحلة ٦)** | ١٢ | `quota.readCap=50000` · `quota.warnAt=70` · `activity.lazyListen=true` · `storage.capMb=1024` |

وخصائص اللوحة المثبتة بالاختبار:

- **لا مفتاحَ بلا مقبض:** كلّ مفتاحٍ في `CATD` و`HISTD` و`QUOTAD` له حقلٌ حيٌّ في DOM — صفر مفاتيح مفقودة.
- **الوصف بلغة العمل لا بلغة البرمجة** — لأنّ من يقرؤه مديرُ مخزنٍ لا مبرمج. وكلّ حقلٍ له تلميحٌ غير فارغ.
- **الحفظ كتابةٌ واحدة لكلّ بطاقة**، بـ`merge:true` — حفظ بطاقةٍ لا يمحو الأخرى.
- **لا كتابةَ قبل ضغط «حفظ»** إطلاقًا.
- **لا يمكن أن يصل `NaN` إلى قاعدة البيانات:** ما فوق السقف يُقصّ إليه (٩٩٩ ← ٩٩)، وما دون الأرضيّة يُرفع إليها (٥ ← ١٠٠)، والكسر يُدوَّر (١٢٫٧ ← ١٣)، والنصّ العربيّ يهبط إلى الأرضيّة لا إلى `NaN`.
- **«إرجاع الافتراضيّات» يعيد الشاشة فقط** ويقول صراحةً إنّ الحفظ ما زال مطلوبًا — ولا يكتب حرفًا.
- **الحارس في الدالّة لا في الزرّ:** موظّفٌ بلا `perms.manage` لا يرى الحقول، **وحتّى لو نادى الحفظ برمجيًّا لم يُكتب شيء**.
- **الأثر فوريّ بلا إعادة تحميل**، وسلسلة التدقيق مسجَّلة (`updatedBy` و`updatedAt`).
- **صفر قراءاتٍ إضافيّة.** أثبتُّه سببيًّا لا بعتبةٍ هشّة: كلفة شاشة الإعدادات **تساوي عدد المستخدمين** (بطاقة الاستثناءات القائمة من قبل)، وتبقى كذلك سواء أكانت الحقول ٤٣ أم صفرًا. القياس: مستخدمان ← ٢ قراءة · أربعة ← ٤ · ستّة ← ٦، **والحقول ٤٣ في الأحوال كلّها**.

**الدليل:** `test_settings_panel.mjs` — ٦٤ تأكيدًا.

---

## ٨ · المرحلة ١١ — التحقّق

| ملفّ الفحص | التأكيدات | ما يغطّيه |
|---|---|---|
| `test_catalog_layer.mjs` | ٣٤ | طبقة التخزين والبحث (ط-١٠) |
| `test_catalog_import.mjs` | ٦٨ | الاستيراد والمطابقة والتقرير (ط-١١ · ط-١٢) |
| `test_catalog_scanadd.mjs` | ١٠٦ | التدفّقان ٤ و٥ (ط-١٣) |
| `test_history_runs.mjs` | ١٢٨ | سجلّ المسح (ط-١٤ · المرحلة ٤) |
| `test_quota_lazy.mjs` | ٧٠ | الحصّة والكفاءة (ط-١٥ · المرحلة ٦) |
| `test_settings_panel.mjs` | ٦٤ | لوحة الإعدادات (ط-١٦) |
| `test_scanflow.mjs` | ٩٣ | محرّك المسح (كان ٩١) |
| **الحزمة كاملة** | **١٬٣٩١ / ١٬٣٩١** | **٥٤ ملفًّا — صفر انحدار** |

وشُغّلت الحزمة **مرّتين كاملتين**: على `index.source.html` الموثّق، وعلى `index.html` المجرَّد من التعليقات — بالأرقام نفسها ملفًّا ملفًّا. أي أنّ نسخة النشر ليست «يُفترض أنّها مطابقة»، بل **مقيسةٌ مطابقة**.

---

## ٩ · انحرافاتٌ عن التصميم المعتمَد — إفصاحٌ كامل

خمسة مواضع نفّذتها خلافًا لحرف الوثيقة المعتمَدة، وأعرضها صريحةً لأنّ الإخفاء أسوأ من الانحراف:

**أ) تقرير الاستيراد مستندٌ واحدٌ بعيّنةٍ محدودة، لا مجموعةً فرعيّة `parts`.** السبب: المستند الواحد يُقرأ بقراءةٍ واحدة، والمجموعة الفرعيّة تُقرأ بقراءةٍ لكلّ جزء. والعيّنة محدودة بـ`import.samplePerSection = 200` لكيلا يتجاوز المستند حدّ الـ١ ميبي‌بايت. **الأثر عليك:** إن استوردت ملفًّا ضخمًا فيه آلاف الأخطاء، فالتقرير يعرض أوّل ٢٠٠ من كلّ قسمٍ ويصرّح بالعدد الكلّيّ. المقبض في يدك.

**ب) لا تقليمَ تلقائيًّا للتقارير القديمة.** بدلًا منه حُدّت القراءة بـ`limit()` و`import.keepReports = 20`. السبب: الحذف التلقائيّ يستهلك حصّة الحذف اليوميّة، وقد يمحو تقريرًا تحتاجه في نزاع. **الأثر:** التقارير تتراكم في قاعدة البيانات بحجمٍ ضئيل، والواجهة تعرض آخر ٢٠.

**ج) `import.reportLayout: 'split'` لم يُنفَّذ** — يُشحن التخطيط الافتراضيّ `one` وحده. السبب: التخطيط المنقسم كان تحسينًا عرضيًّا لا متطلّبًا، وتنفيذه يضاعف مسار الطباعة بلا حاجةٍ مثبتة.

**د) سجلّ الحركة يسجّل سطرًا لكلّ تتابعٍ متّصل لا لكلّ مسحة** (مجمَّعًا حتّى `history.actFlushMs = 2500`). السبب: الكلفة — سطرٌ لكلّ مسحة يعني كتابةً لكلّ مسحة، وهو ما منعتَه صراحةً في المرحلة ٦. **والتقارير تبقى متطابقةً عدديًّا**، لأنّها لا تُحسب من السجلّ أصلًا (المرحلة ٨). المقبض: `history.activityMerge`.

**هـ) مستمع الحركة يفتح عند الطلب لا عند الإقلاع.** السبب: خفض قراءات الإقلاع. **قابلٌ للإرجاع** بإطفاء `quota.activity.lazyListen` من الإعدادات.

---

## ١٠ · ما لم أفعله — الممنوعات كما تركتها

- لم أغيّر أيّ **قاعدة عملٍ** للجرد.
- لم أغيّر أيّ **حساب**.
- لم أغيّر أيّ **تقرير**.
- لم أغيّر أيّ **صلاحيّة**، ولم أستحدث واحدة — أعدتُ استعمال القائم.
- لم أُدخل أيّ **خطوةٍ يدويّةٍ إضافيّة**.
- لم أُلزم العامل بنقرةٍ بعد أيّ مسحة. **الماسح يبقى جاهزًا للباركود التالي دائمًا.**
- لم ألمس `firestore.rules` منذ ط-١٠.
- لم أضع سرًّا واحدًا في الشيفرة ولا في التقارير ولا في المحادثة.

---

## ١١ · الأرقام في سطر

```
التطبيق (نشر)   : 685,706 بايت   ·  مضغوطًا 180,491
التطبيق (مصدر)  : 791,750 بايت   ·  مضغوطًا 217,444
الوفر بالتجريد  : 106,044 خامًا  ·  36,953 مضغوطًا (16.9٪)
القواعد         : 28,833 / 262,144 بايت (11.0٪) — بلا تغيير
أثقل فرع قواعد  : 3,705 — بلا تغيير      ·  get مميّز: 3/10 — بلا تغيير
الاختبارات      : 1,391 / 1,391 — ٥٤ ملفًّا — خضراء مرّتين
مفاتيح الإعدادات: 43 مفتاحًا — كلّها بمقابض ظاهرة
```
