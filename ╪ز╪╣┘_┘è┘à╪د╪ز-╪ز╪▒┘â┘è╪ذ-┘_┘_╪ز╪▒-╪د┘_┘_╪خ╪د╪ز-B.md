# فلتر الفئات المتعدّد (النمط B) — تركيبٌ وتشغيل · ط-١٧

**الحزمة:** `iomp-فلتر-الفئات-B.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1507/1507 — ALL GREEN ✓` (٥٥ ملفًّا · +١٧ فحصًا في `test_catfilter.mjs`) · **`firestore.rules` بلا تغيير** · **حجم النشر ١٨٨٬٠٢٨ بايت مضغوطًا** (تحت سقف ٢٠٠٬٠٠٠).

---

## ١ · ما الذي تغيّر

استُبدلت قائمة الفئات الأحاديّة `<select id="repCat">` في **شاشة تقرير الفروقات** بلوحة **النمط B** الذي اعتمدتَه: **بحث + قائمة صناديق + رقائق**، لوحةٌ ظاهرةٌ دائمًا. لا مساس بأيّ شيءٍ آخر في الشاشة.

**السلوك:**

- **بحثٌ فوريّ بتطبيعٍ عربيّ** (كـ`arNorm` نفسها في بقيّة النظام): أإآ→ا، ة→ه، ى→ي، تشكيل، بلا حساسيّة حالة، واحتواء (contains). فالبحث عن «سجاد» يجد «سجّاد تركي» و«سجّاد صيني» و«سجّاد مسجد».
- **‏<span dir="ltr">Enter</span> = اختيار كلّ النتائج المطابقة** (قراراك المعتمَد): «سجاد» + <span dir="ltr">Enter</span> يختار كلّ فئات السجّاد دفعةً واحدة.
- **رقائقُ المختار قابلةٌ للإزالة** (✕ على كلّ رقاقة) + **عدّادٌ** يُظهر عددها.
- **زرّان:** «✓ اختر كلّ النتائج (N)» و«مسح الكلّ».
- **لوحة المفاتيح كاملة:** <span dir="ltr">Enter</span> (كلّ النتائج) · <span dir="ltr">↑↓</span> تنقّل · مسافة تبدّل المظلَّل · <span dir="ltr">Backspace</span> على بحثٍ فارغٍ يزيل آخر رقاقة · <span dir="ltr">Esc</span> يمسح البحث.
- **الحالة الفارغة = كلّ الفئات** (بلا فلتر) — تمامًا كـ«كل الفئات» سابقًا.

---

## ٢ · ما الذي **لم** يتغيّر (تعهّدات السلامة)

- **الحسابات دون مساس:** الفلتر واجهةٌ فقط؛ `computeVariance`/`summarize` بلا أيّ تغيير. البلاطات العلويّة تبقى على إجمالي كلّ الأصناف كما كانت (الفلتر يضيّق الجدول لا البلاطات) — سلوكٌ مطابقٌ للأحاديّ السابق (`repCat.onchange=renderReportRows`).
- **الطباعة والتصدير متّسقان تلقائيًّا:** يقرآن الاختيار عبر `filteredReportRows` نفسها، فطباعة «مراجعة الفرع» وتصدير Excel يحترمان الفئات المختارة بلا أيّ تغييرٍ في تنسيقهما.
- **لا مساس بمسار العدّ ولا الماسح ولا الصلاحيات** — التغيير محصورٌ في شاشة التقارير.
- **مطابقة الفئة نفسها حرفيًّا** كالأحاديّ السابق (`r.category`) — لا رفعٌ ولا خفضٌ في الأصناف الظاهرة لأيّ فئةٍ مختارة.

---

## ٣ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.source.html` · `index.html` | لوحة النمط B بدل `repCat`، حالة `repCatSel`/`repCatQ`، دوالّ `renderCatFilter`/`catfRenderList`/`catfKey`…، تحديث `filteredReportRows`، وCSS `.catf` (RTL ومتجاوب) |
| `build_harness.js` | خطّافات `window.__catf` (بحث/تبديل/رقائق/عدّاد/لوحة مفاتيح/صفوف مُصفّاة) |
| `test_catfilter.mjs` | ملفٌّ جديد — ١٧ فحصًا (المجموعة ط) |
| `run_all_tests.mjs` | تسجيل الملفّ الجديد في الحزمة |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدد (١٬٤٩٠ ← ١٬٥٠٧) و(٥٤ ← ٥٥ ملفًّا) |
| `تعليمات-تركيب-فلتر-الفئات-B.md` | هذه الوثيقة |

---

## ٤ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp`.
3. `node build_release.js` — يجب أن يُنهي بـ **«الشيفرة متطابقةٌ حرفًا بحرف ✓»**.
4. `node build_harness.js` ثمّ `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 1507/1507 — ALL GREEN ✓`**.
5. ادفع إلى `main` — يتكفّل CI ببقيّة الفحوص والنشر.

**لا تغيير في أيّ حساب ولا قاعدة ولا صلاحيّة.**

---

## ٥ · لاحقًا (مسار التوسّع المتّفق عليه)

حين تبلغ الفئات **المئات أو تصير متشعّبة**، نرقّي B إلى **D** (تجميعٌ بالعائلات + «اختر العائلة» + طيّ) — الانتقال طبيعيٌّ لأنّ B وD يتشاركان البحث والرقائق والعدّاد. أخبرني حين تريد ذلك.
