# تعليمات رفع تحديث إجراءات خطّ التكامل — إسكات تحذيرات Node 20

**الحزمة:** `iomp-تحديث-إجراءات-CI.zip` · **التاريخ:** ٢٦ يوليو ٢٠٢٦
**ملفّان فقط** — ولا يمسّان التطبيق ولا الاختبارات ولا القواعد بحرفٍ واحد.

---

## ١ · ما التحذير، ولماذا يستحقّ الإصلاح فعلًا

التحذيرات الثلاثة في التشغيلة ٦٨ نصّها واحد:

> `Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24`

أي أنّ الإجراءات التي تستعملها مكتوبةٌ لتعمل على Node 20، وGitHub **يجبرها الآن** على Node 24. وهذا الإجبار ليس دائمًا: في إعلان GitHub الرسميّ أنّ المشغّلات بدأت تستعمل Node 24 افتراضيًّا **منذ ١٦ يونيو ٢٠٢٦**، وأنّ **Node 20 يُزال من المشغّل في خريف ٢٠٢٦**.

فالتحذير اليوم **تحذيرٌ لا خطأ** — خطّك أخضرُ تحته بدليل التشغيلة ٦٨ — لكنّه **موعدٌ لا رأيٌ**. إذا بقي على حاله فقد ينكسر خطّ التكامل في الخريف من تلقاء نفسه دون أن تغيّر أنت سطرًا. ولهذا التحديث الآن — في وقتٍ هادئٍ وخطٍّ أخضر — أهون من التحديث تحت ضغط انكسارٍ مفاجئ.

---

## ٢ · ما غيّرتُه بالضبط

**عشرة أسطر، كلّها أرقام إصدارات، لا غير.** لم تتغيّر خطوةٌ واحدة، ولا وسيطٌ واحد، ولا أمرٌ واحد.

| الإجراء | كان | صار | التحقّق |
|---|---|---|---|
| `actions/checkout` | `@v4` | **`@v6`** | `runs.using: node24` ✓ |
| `actions/setup-node` | `@v4` | **`@v6`** | `runs.using: node24` ✓ |
| `actions/setup-java` | `@v4` | **`@v5`** | `runs.using: node24` ✓ |
| `actions/upload-pages-artifact` | `@v3` | **`@v5`** | مركَّبٌ يستدعي `upload-artifact@v7.0.0` ✓ |
| `actions/deploy-pages` | `@v4` | **`@v5`** | `runs.using: node24` ✓ |

ولم أنقل هذه الأرقام عن ذاكرةٍ ولا تخمين: قرأتُ **ملفّ `action.yml` لكلّ إجراءٍ على وسمه الجديد** وتحقّقتُ بعينيَّ من قيمة `runs.using` ومن بقاء كلّ وسيطٍ نستعمله صالحًا.

### الملفّان

- **`.github/workflows/ci.yml`** — ثمانية استبدالات (٣ × checkout، ٢ × setup-node، setup-java، upload-pages-artifact، deploy-pages). ويحمل معه تصحيح اسم المهمّة من «(٤٣٥)» إلى «(١٣٩١)».
- **`.github/workflows/backup.yml`** — استبدالان (checkout، setup-node). **ملفّ النسخ الاحتياطيّ اليوميّ إلى Dropbox كان يحمل التحذير نفسه صامتًا**، لأنّه يعمل بجدولٍ ليليّ لا تراه في تبويب Actions إلّا إن قصدته. فأصلحتُه معه كي لا تفاجأ بانقطاع نسخك الاحتياطيّة في الخريف.

---

## ٣ · لماذا هذه الأرقام تحديدًا لا غيرها

**التحذير الثالث كان الأصعب.** انظر نصّه: يشكو من `actions/upload-artifact@v4` — ونحن **لا نستدعي هذا الإجراء أصلًا** في `ci.yml`. فمن أين جاء؟

جاء من الداخل: `upload-pages-artifact` إجراءٌ **مركَّب** (composite)، أي أنّه يستدعي إجراءاتٍ أخرى في جوفه، ومنها `upload-artifact`. فرفعُ إصدارِ ما تراه في ملفّك لا يكفي؛ كان لا بدّ من إصدارٍ من `upload-pages-artifact` يحمل في جوفه نسخةً حديثةً من التابع. وقد تحقّقت من ذلك بقراءة `action.yml` للوسم `v5` فوجدتُ فيه صراحةً `actions/upload-artifact@…#v7.0.0`. **هذا هو السطر الذي يُسكت التحذير الثالث، ولا يُسكته شيءٌ سواه.**

---

## ٤ · تغييران في السلوك أُنبّهك إليهما — وكلاهما لا يمسّك

الصدق أولى من الطمأنة، فإليك ما تغيّر في هذه الإصدارات مّما قد يمسّ مشاريع أخرى:

1. **`setup-node@v6`** حصر التخزين المؤقّت التلقائيّ في npm وحده. **لا يمسّك**: ملفّاك لا يستعملان الوسيط `cache` إطلاقًا.
2. **`checkout@v6`** صار يحفظ بيانات الاعتماد في ملفٍّ منفصل بدل تضمينها في `.git/config`. **لا يمسّك**: لا خطوة عندك تقرأ الرمز من إعدادات git.
3. **`upload-pages-artifact@v5`** يستبعد الملفّات المخفيّة افتراضيًّا. **لا يمسّك**: مجلّد `_site` يُبنى بنسخ ثمانية ملفّاتٍ مسمّاةٍ صراحةً، وليس فيها ملفٌّ مخفيّ.

---

## ٥ · خطوات الرفع

الملفّان في الحزمة داخل مسارهما الصحيح جاهزَين. ارفعهما كما ترفع أيّ ملفّ:

1. افتح المستودع ← زرّ **Add file** ← **Upload files**.
2. اسحب مجلّد **`.github`** من الحزمة المفكوكة كما هو (يحمل `workflows/ci.yml` و`workflows/backup.yml`).
   - إن رفعت الملفّين منفردين فتأكّد أنّ كلًّا منهما ذاهبٌ إلى `.github/workflows/` — لا إلى جذر المستودع.
3. اكتب في وصف الحفظ مثلًا: `تحديث إجراءات CI إلى Node 24` ثمّ **Commit changes**.

الدفع إلى `main` يُشغّل الخطّ تلقائيًّا.

---

## ٦ · كيف تعرف أنّه نجح

في صفحة التشغيلة الجديدة تنظر إلى موضعين:

- **المهامّ الثلاث خضراء** كما في التشغيلة ٦٨، والمدّة نحو عشر دقائق.
- **صندوق `Annotations` اختفى** — أو صار «٠ warnings» بدل «3 warnings». هذا هو الدليل المقصود.

وإن رأيت تحذيرًا باقيًا فانسخ نصّه لي: سيكون قد كشف إجراءً رابعًا مختبئًا داخل إجراءٍ مركَّبٍ آخر، وهو مكشوفٌ بالقراءة كما كُشف الثالث.

---

## ٧ · الرجوع للخلف

لا حاجة إلى نسخةٍ احتياطيّة: التغيير عشرة أرقام. افتح الملفّ في GitHub واضغط قلم التحرير وأعِد `@v6` إلى `@v4` و`@v5` إلى `@v4` و`@v5` إلى `@v3` في `upload-pages-artifact`. أو استعمل تبويب **History** على الملفّ وارجع إلى النسخة السابقة بضغطة.

---

## ٨ · ما لم أفعله ولماذا

- **لم أرفع `node-version: 22`** في خطوات الاختبار. هذا هو Node الذي شُغّلت عليه الحزمة كلّها (١٬٣٩١ فحصًا) ومرّت خضراء، وهو منفصلٌ تمامًا عن Node الذي تعمل عليه الإجراءات نفسها. **رفعُه تغييرٌ في بيئة الاختبار لا في التحذير**، ولا يصحّ أن أخلط إصلاحًا بمخاطرةٍ لم تطلبها.
- **لم أثبّت الإجراءات على تجزئة (SHA)** رغم أنّه أمتنُ أمنيًّا من الوسم، لأنّه يقلب صيانة الملفّ رأسًا على عقب (كلّ ترقيةٍ تصير يدويّة). إن أردتَه فقل، وأعطيك الملفّين مثبَّتَين بالتجزئات.
