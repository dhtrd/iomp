# تعليمات تركيب حزمة ط-١٠ — طبقة الكتالوج الرئيسيّ

**الحزمة:** `iomp-ط١٠-طبقة-الكتالوج.zip` · **التاريخ:** ٢٥ يوليو ٢٠٢٦

---

## ١ · ما في الحزمة

| الملفّ | الوجهة | نوع التغيير |
|---|---|---|
| `index.html` | جذر المستودع — يستبدل القائم | مُعدَّل: كتلة ط-١٠ (٦٣ سطرًا) |
| `firestore.rules` | جذر المستودع — يستبدل القائم | مُعدَّل: ٣ كتل `match` جديدة |
| `firestore-rules.test.mjs` | جذر المستودع — يستبدل القائم | مُعدَّل: ٢١ تأكيدًا جديدًا |
| `build_harness.js` | جذر المستودع — يستبدل القائم | مُعدَّل: بذر + محاكاة عطل + خطّاف |
| `run_all_tests.mjs` | جذر المستودع — يستبدل القائم | مُعدَّل: تسجيل الاختبار الجديد |
| `test_catalog_layer.mjs` | جذر المستودع — **ملفّ جديد** | جديد |
| `measure_catalog.mjs` | جذر المستودع — **ملفّ جديد** | جديد (أداة قياس لا اختبار) |
| `٨-تسليم-طبقة-الكتالوج-ط-١٠.md` | مجلّد التقارير | جديد |

**لا تحذف أيّ ملفٍّ قائم.** الحزمة لا تُسقط شيئًا.

---

## ٢ · خطوات التركيب

1. **خذ نسخةً احتياطيّة** من `index.html` و`firestore.rules` قبل الاستبدال.
2. فُكّ الحزمة فوق جذر المستودع (استبدالٌ مباشر).
3. `node build_harness.js` — يعيد بناء `harness.html` من `index.html` الجديد.
4. `node run_all_tests.mjs` — يجب أن يُنهي بـ **`TOTAL: 953/953 — ALL GREEN ✓`** (يستغرق ٦–٧ دقائق).
5. ادفع إلى الفرع ودع الـ**CI** يحكم على `firestore.rules` — المُحاكي محجوبٌ عندي، والـCI هو الحكم الوحيد كما اتّفقنا. ينبغي أن يُنهي بـ `RECON 121/121`.
6. **لا تنشر القواعد يدويًّا.** انتظر خضرة الـCI.

---

## ٣ · ماذا يتغيّر في سلوك التطبيق بعد التركيب؟

**لا شيء — وهذا مقصود.**

الطبقة **مبنيّةٌ ولم تُوصَّل بعد**: لا شيء ينادي `catalogEnsure()`، و`scanStep` لم تُربط بـ`catalogFind`. المستخدم لن يرى فرقًا واحدًا. التوصيل يأتي في ط-١٣ بعد بناء واجهة الرفع في ط-١١.

بنيتُ **القارئ** أوّلًا وأثبتُّه بالقياس، ليُبنى **الكاتب** فوق أساسٍ مقاسٍ لا مفترض.

---

## ٤ · قواعد الأمان — ما أُضيف وما لم يُمَسّ

أُضيفت ثلاث مجموعاتٍ جديدة فقط، قبل قاعدة الرفض النهائيّة مباشرةً:

```
catalogMeta/{doc}       قراءة: كل مستخدمٍ نشط · كتابة: warehouse.manage · حذف: trash.purge
catalogChunks/{chunk}   قراءة: كل مستخدمٍ نشط · كتابة: warehouse.manage · حذف: trash.purge أو warehouse.manage
catalogImports/{jid}    قراءة: warehouse.manage أو report.view · إنشاء: بالفاعل نفسه · تعديل: ممنوع مطلقًا · حذف: trash.purge
```

**لم يُمَسّ أيّ سطر `allow` قائم، ولا أيّ دالّة، ولا أيّ صلاحيّة.** أُعيد استعمال صلاحيّتين قائمتين بلا استحداث واحدة. وفي حزمة الاختبار تأكيدٌ حارس يثبت أنّ العدّاد **ما زال ممنوعًا من `products`** كما كان قبل التغيير.

**القياس بعد التعديل:** أثقل فرع ٣٬٧٠٥ (بلا تغيير) · أقصى `get` مميّز ٣/١٠ (بلا تغيير) · نصّ القواعد ١١٫٠٪ من الحدّ · قراءة العدّاد للكتالوج ٣٣ تعبيرًا مقابل ١٬٠٢٤ لقراءة `snapshot/{chunk}` القائمة في الإنتاج اليوم.

---

## ٥ · التحقّق اليدويّ السريع (اختياريّ)

بعد `node build_harness.js`، افتح `harness.html` في متصفّح وشغّل في وحدة التحكّم:

```js
await window.__cat.ensure();     // {ok:true, source:'empty'|'cloud', reads:1, ...}
window.__cat.reads();            // عدد القراءات المستهلَكة
window.__cat.find('123456');     // null ما لم يُرفع كتالوج بعد
window.__cat.chunkSize();        // 400
```

القيمة المهمّة: **`reads()` تبقى صفرًا مهما بحثت.** البحث لا يمسّ Firestore إطلاقًا.

---

## ٦ · ملاحظة أمانةٍ واجبة

فحص الأسرار على الملفّات **الجديدة** نظيف تمامًا. وفي `index.html` القائم تطابقٌ واحدٌ هو **إعداد Firebase للويب** الموجود منذ اليوم الأوّل — وهو مُعرِّفٌ عامٌّ بحكم التصميم يُشحن إلى كلّ متصفّح، والحماية الحقيقيّة قائمةٌ على قواعد Firestore لا على إخفائه. **لم أُضِفه ولم ألمسه.** أذكره لك لأنّ الشفافيّة أولى، لا لأنّه ثغرة.

---

## ٧ · الرجوع للخلف

استبدل `index.html` و`firestore.rules` بنسختك الاحتياطيّة، واحذف `test_catalog_layer.mjs` و`measure_catalog.mjs`، وأعِد `run_all_tests.mjs` و`build_harness.js` و`firestore-rules.test.mjs`. لا تبقى أيّ آثارٍ في قاعدة البيانات — الطبقة **لا تكتب حرفًا** (وهذا مُثبَتٌ باختبارٍ صريح).
