# تعليمات: إصلاح قارئ الباركود — لا يسجّل العدّ (م٦-٣)

## المشكلة
قارئ الباركود لا يقرأ العدّ ولا يسجّله. سببان:
1. **الماسح الخارجي (keyboard-wedge / بلوتوث)** يكتب الكود في حقل البحث ثم يرسل Enter — لكن التسجيل كان **مشروطًا بتفعيل «وضع الماسح»** يدويًّا؛ فمن يمسح دون تفعيله لا يُسجَّل عدّه.
2. **مطابقة صارمة للكود**: إن اختلف الباركود الممسوح عن الكود المخزّن بأصفار بادئة أو تنسيق (EAN)، لا يُطابَق الصنف فيظهر «غير موجود» أو لا شيء.

## الإصلاح
1. **تسجيل تلقائي بالماسح الخارجي دون «وضع الماسح»**: عند كتابة/مسح كودٍ في حقل البحث والضغط Enter، إن **طابق القيمة صنفًا** (بالكود أو الباركود) يُسجَّل **عدّة ١+** فورًا بصوت، ويُفرَّغ الحقل ليمسح التالي — بلا حاجة لأي زر. (البحث النصّي العادي + Enter لا يُنشئ عدّة إن لم يطابق صنفًا.)
2. **مطابقة متسامحة للباركود** (`findByScan`): مباشرة ← تجاهل **الأصفار البادئة** ← مقارنة **الأرقام فقط** للأكواد الطويلة (≥٤ خانات) — على **الكود والباركود** معًا، مع تشذيب المسافات. فيُطابَق «016580» ← «16580»، و«12345» ← «0012345»، والباركود «6281000123456» ← كود صنفه.
3. **«وضع الماسح» يبقى كما هو** (لا انحدار) — مفيدٌ لإبقاء التركيز على الحقل أثناء سلسلة مسح.

## الملفات في هذه الحزمة (استبدل/أضف في جذر المستودع dhtrd/iomp)
| الملف | الحالة |
|---|---|
| `index.html` | **استبدال** — مطابقة متسامحة + تسجيل بالماسح الخارجي على Enter |
| `build_harness.js` | **استبدال** — خطّاف فحص `__findByScan` |
| `run_all_tests.mjs` | **استبدال** — أُضيف `test_scanfix.mjs` |
| `test_scanfix.mjs` | **جديد** — ١٣ فحصًا (المطابقة المتسامحة + التسجيل بالماسح + عدم إنشاء عدّة خاطئة + بقاء وضع الماسح) |
| `README.md` | **استبدال** — تحديث الأرقام (٨٢٢ فحصًا / ٤٧ ملفًا) |

## خطوات الرفع
1. ارفع الملفات إلى الجذر (Add file → Upload files) واعتمد (Commit) إلى main.
2. CI: فحوص التطبيق المتوقعة **٨٢٢/٨٢٢** + فحوص القواعد **١٠٠/١٠٠** ثم النشر.

## ⚠️ قواعد الأمان
**لم يتغيّر `firestore.rules`** — لا نشر في وحدة تحكم Firebase.

## للتجربة بعد الرفع
- بجهاز ماسح: افتح شاشة العد، وامسح باركود صنفٍ من الجلسة مباشرةً (دون الضغط على أي زر) — يُسجَّل عدّة ١+ بصوت، وتتراكم مع كل مسح.
- إن كان الماسح لا يرسل Enter تلقائيًّا: فعّل خيار «إضافة Enter/CR بعد المسح» من إعدادات جهاز الماسح.
- بالكاميرا: زر «📷 مسح بالكاميرا» يعمل عبر قارئ المتصفح المدمج أو ZXing (يتطلّب إذن الكاميرا واتصالًا لأول تحميل).

## أرقام الفحوص
- المحلي: **TOTAL: 822/822 — ALL GREEN ✓** (٤٧ ملفًا؛ صفر انحدار)
- CI قواعد الأمان: بلا تغيير (100/100)
