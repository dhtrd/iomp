# تعليمات التسليم — إعادة تصميم محرّك مسح الباركود (م٦-٤)

**التاريخ:** ٢٥ يوليو ٢٠٢٦ • **الحالة:** جاهز للرفع • **الاختبارات:** ٨٩٨/٨٩٨ خضراء

---

## ١) ما الذي تغيّر باختصار

كان مسار المسح السابق **يفقد المسحات صمتًا**: إن لم يُرسل الماسح مفتاح Enter بعد الباركود، لا يُسجَّل العدّ ولا تظهر أي رسالة خطأ — القياس أثبت **٠ من ٨ مسحات مُسجَّلة**. أُعيد بناء المسار من الأساس في ثماني طبقات، والنتيجة **٨/٨ مسحات مُسجَّلة بلا Enter**، وزمن استجابة انخفض من ٣٠٫٤ إلى **١٫١ مللي‌ثانية** في المسار المُعتاد.

سير العمل الآن مطابق للمواصفة حرفيًّا: يقرأ الباركود ← يبحث فورًا ← يزيد الكمية تلقائيًّا ← يُحدّث المعروض ← يؤكّد النجاح ← يجهّز نفسه للمسحة التالية. **بلا تأكيد يدوي، بلا نقر، بلا Enter، بلا تأخير.**

التفاصيل الكاملة في التقارير الستة المرفقة.

---

## ٢) ملفات الحزمة — أين يذهب كلّ ملف

ارفع إلى **جذر المستودع** `dhtrd/iomp`:

| الملف | الحالة | الغرض |
|---|---|---|
| `index.html` | **استبدال** | التطبيق كاملًا — محرّك المسح الجديد بطبقاته الثماني |
| `build_harness.js` | **استبدال** | خطّافات فحص جديدة للمحرّك (`__scanCommit`, `__scanQueueLen`, `__scanIndexStats`) |
| `run_all_tests.mjs` | **استبدال** | أُضيف `test_scanflow.mjs` إلى قائمة الملفات |
| `test_scanflow.mjs` | **جديد** | ٧٠ تأكيدًا لمحرّك المسح |
| `measure_ba.mjs` | **جديد** | أداة قياس «قبل/بعد» — لا تعدّل `index.html` إطلاقًا |
| `README.md` | **استبدال** | تحديث الأرقام (٨٩٨ اختبارًا / ٤٨ ملفًا) + أمر القياس |

**ملفّات مرجعيّة لا تُرفع** (للأرشيف عندك): `bench-before.json`, `bench-after.json`, `bench-summary.json`, ومجلّد `تقارير-م٦-٤/`.

---

## ٣) خطوات الرفع

1. في GitHub: **Add file → Upload files** إلى جذر المستودع.
2. اسحب الملفات الستّة أعلاه، ثم **Commit** إلى `main`.
3. راقب CI: المتوقّع **٨٩٨/٨٩٨** لاختبارات التطبيق، ثمّ اختبارات القواعد، ثمّ النشر التلقائي.

### ⚠️ قواعد الأمان
**`firestore.rules` لم يتغيّر في هذه المرحلة.** لا حاجة لأي نشر في وحدة تحكّم Firebase. CI وحده هو الحَكَم على القواعد كالمعتاد.

### ⚠️ الأسرار
لا مفاتيح ولا أسرار في هذه الحزمة ولا في التطبيق. كل الثوابت المُضافة أرقام ضبط زمنيّة.

---

## ٤) قراراتك المُطبَّقة في هذه النسخة

| القرار | القيمة المُطبَّقة | أين |
|---|---|---|
| سقف الكمية اليدوية قبل طلب تأكيد | **١٠٠** | `MANUAL_QTY_CAP` |
| نافذة منع الازدواج للماسح السلكي | **١٢٠ مللي‌ثانية** | `SCAN_DEDUP_MS` |

القيمتان محروستان باختبار: لو غُيّرت إحداهما سهوًا في أي تعديل لاحق، تسقط الحزمة فورًا.

> **ملاحظة على سقف الـ١٠٠:** يخصّ **الإدخال اليدوي فقط** (زر ➕ أضف). المسح يبقى ‎+١‎ دائمًا وبلا أي تأكيد — لا يمسّه هذا السقف إطلاقًا.

---

## ٥) الاستثناء الوحيد المُفصَح عنه في مسار الكتابة

إفصاحًا كاملًا: هذا هو **التعديل الوحيد** خارج طبقتَي الالتقاط والعرض.

```js
// داخل countWriteAdd
if (eid != null && (cur.entries || []).some(e => e && e.id === eid)) return;
```

**لماذا:** الطبقة ط-٣ تُعيد محاولة الكتابة حتى ثلاث مرّات عند الفشل. بلا هذا الحارس، محاولةٌ نجحت ثمّ أُعيدت (لانقطاع اتصال أثناء تأكيد الكتابة) كانت ستُنتج **كمية مضاعفة**. هذا الحارس هو ما يجعل ضمان «زيادة واحدة بالضبط» صحيحًا فعلًا.

**أثره على قواعد الجرد:** لا شيء. لا يغيّر أي حساب، ولا أي فرق، ولا أي عجز، ولا أي تقرير، ولا أي مطبوعة، ولا أي صلاحية. وهو **مشروط بـ `eid != null`**، أي أن كل الإضافات القديمة والإدخال اليدوي (بلا `eid`) لا يمسّها إطلاقًا — سلوكها اليوم هو سلوكها بالأمس بالضبط. الحالة التي يمنعها لا تحدث في التشغيل العادي؛ هي حصريًّا نتيجة إعادة محاولة.

الاختبار الحارس: `ع٦` في `test_scanflow.mjs`.

---

## ٦) ما لم يُمسّ

| البند | الحالة |
|---|---|
| قواعد الجرد وحسابات الكميات والفروقات والعجز | لم تُمسّ |
| التقارير والمطبوعات | لم تُمسّ |
| الصلاحيات (`firestore.rules` + الفرض في الواجهة) | لم تُمسّ |
| معرّفات العناصر (IDs) واتجاه RTL | لم يُعَد تسمية أي معرّف |
| «وضع الماسح» 🔫 والكاميرا 📷 | يعملان كما كانا (لا انحدار) |

---

## ٧) كيف تجرّب بعد الرفع — خمس تجارب قصيرة

**١. المسحة العادية (بلا Enter):** افتح شاشة العدّ، امسح باركود صنف في الجلسة **دون الضغط على أي زر**. المتوقّع: لوحة خضراء فورًا فيها الحقول الستّة (الاسم، الباركود، الكمية الحالية، المضافة ‎+١‎، الإجمالي بعد التحديث، مؤشّر النجاح) + صوت نجاح + اهتزاز قصير، والحقل يعود نظيفًا جاهزًا.

**٢. التكرار:** امسح **الصنف نفسه ثلاث مرّات متتالية بسرعة**. المتوقّع: ١ ← ٢ ← ٣ بلا أي نافذة تأكيد وبلا تباطؤ.

**٣. الباركود المجهول:** امسح باركودًا ليس في الجلسة. المتوقّع: لوحة حمراء ⛔ + نغمة فشل + الرسالة «هذا الباركود غير موجود في جلسة الجرد الحالية…» — **دون تجميد**؛ الماسح يبقى جاهزًا للتالي فورًا.

**٤. الصنف المضاف حديثًا:** من التنبيه أعلاه اضغط «➕ صنف يدوي» (الباركود مُعبّأ مسبقًا)، أضِفه، ثم **امسحه مباشرةً**. المتوقّع: يُعرَف من أول مسحة.

**٥. الرشقة السريعة:** امسح عشرين صنفًا مختلفًا بأسرع ما تستطيع. المتوقّع: **عشرون عدّة، لا واحدة أقلّ**، وشاشة لا ترتعش.

> إن كان ماسحك يرسل Enter تلقائيًّا فلا مشكلة إطلاقًا — المحرّك يتعامل مع الحالتين، ويُهمِل صدى CR+LF المزدوج تلقائيًّا.

---

## ٨) الأرقام الرئيسية (قبل ← بعد)

| القياس | قبل | بعد |
|---|---|---|
| مسح بلا Enter — مُسجَّل | **٠ / ٨** | **٨ / ٨** |
| زمن التأكيد (مع Enter، وسيط) | ٣٠٫٤ مللي | **١٫١ مللي** |
| أسوأ زمن تأكيد مُسجَّل | ١٠٨٫٣ مللي | **١٫٦ مللي** |
| البحث عن باركود غير موجود (٨٠٠٠ صنف) | ٢٫٥٣٥٥ مللي | **٠٫٠٠٠٢ مللي** |
| تنبيه الباركود المجهول | ٢٣٫٢ مللي | **٠٫٩ مللي** |
| رشقة ٢٠ مسحة — إعادات رسم كاملة | ٢٦٠ | **٢٠** |
| رشقة ٢٠ مسحة — زمن الحائط | ٦٨٫٦ ثانية | **١١٫٠ ثانية** |
| ٥٠ مسحة في نبضة واحدة | **غير مدعوم** | ٥٠/٥٠ بمعدّل ٨٫٢٢ مللي للمسحة |

المنهج والتفصيل الكامل في `٤-تقرير-الأداء.md` — بما فيه توضيح خطأ قياس اكتُشف أثناء العمل وأُصلح قبل التسليم.

---

## ٩) التقارير الستة في هذه الحزمة

| الملف | المحتوى |
|---|---|
| `١-تقرير-التدقيق.md` | سبعة إخفاقات مُرتَّبة في المسار السابق، كلٌّ مدعوم بقياس |
| `٢-تقرير-الأسباب-الجذرية.md` | ستة أسباب جذرية وخريطة سبب ← طبقة ← دليل |
| `٣-تقرير-إعادة-التصميم.md` | مخطّط المسار الجديد، الطبقات الثماني، جدول الثوابت، وما لم يُمسّ |
| `٤-تقرير-الأداء.md` | المنهج أوّلًا، ثم جداول قبل/بعد كاملة |
| `٥-تقرير-الواجهة.md` | الحقول الستة، الحالات الثلاث، الباركود المجهول، الإتاحة وRTL |
| `٦-تقرير-التحقّق.md` | ٨٩٨/٨٩٨، خريطة ضمان ← إثبات، والتزام القيود بندًا بندًا |

---

## ١٠) إعادة التشغيل عندك

```bash
npm install
export CHROME_EXE=$(node -e "console.log(require('playwright-core').chromium.executablePath())")

node run_all_tests.mjs                                 # الحزمة كاملة — المتوقّع 898/898
SRC_HTML=index.html BA_TAG=after node measure_ba.mjs   # قياس النسخة الحالية
```

أداة القياس تقرأ الملف عبر متغيّر البيئة `SRC_HTML` وتكتب ناتجها في `bench-<TAG>.json` — **لا تكتب فوق `index.html` بأي حال**.
