# تفعيل «حجم خط الطباعة» للتحكّم بعدد الصفحات — ط-١٩

**الحزمة:** `iomp-حجم-خط-الطباعة.zip` · **التاريخ:** ٢٧ يوليو ٢٠٢٦
**الاختبارات:** `TOTAL: 1521/1521 — ALL GREEN ✓` (٥٥ ملفًّا · +٥ فحوص طباعة) · **`firestore.rules` بلا تغيير** · **حجم النشر ١٨٩٬٤١٧ بايت مضغوطًا** (تحت سقف ٢٠٠٬٠٠٠).

---

## ١ · المشكلة

في **الإدارة ← إعدادات المخرجات ← الطباعة** يوجد حقل **«حجم الخط (نقطة)»** — لكنّه كان **بلا أثرٍ** على أهمّ المطبوعات («مراجعة الفرع» و«التقرير المفصّل»): كانت تلك المطبوعات تحسب البنط **تلقائيًّا** حسب عدد الأعمدة (إصلاح م٦ ضدّ قصّ الأعمدة) وتتجاهل قيمتك. فتغييره لم يكن يزيد الصفحات ولا ينقصها.

## ٢ · الحلّ

جعلتُ الحقل **فعّالًا على كلّ المطبوعات** (مراجعة الفرع · المفصّل · المحاضر · التقارير · الكتالوج):

- **رقمٌ صريح** (مثلًا ٨) ⇒ يُطبَّق حرفيًّا: نصٌّ أصغر **وحشوٌ أضيق** (النصّ وغيره) ⇒ صفوفٌ أكثر في الصفحة ⇒ **صفحاتٌ أقلّ**. ورقمٌ أكبر (مثلًا ١٣) ⇒ **صفحاتٌ أكثر**. فبيدك الآن زيادة الصفحات أو إنقاصها حسب الحاجة.
- **فارغ = «تلقائي»** ⇒ يعود للاحتساب الذكيّ حسب عدد الأعمدة (سلوك م٦ الذي يمنع قصّ الأعمدة). هذا هو الوضع الافتراضيّ، فلا تتغيّر مطبوعاتك الحاليّة ما لم تختر رقمًا.
- **بلا قصّ مهما كبر الخط:** الجدول يبقى ضمن عرض الورقة دائمًا (‏`table-layout:fixed` + أوزان أعمدة) — الخطّ الأكبر يزيد الالتفاف والصفحات فقط، لا يقصّ عمودًا.

**كيف تستعمله:** الإدارة ← إعدادات المخرجات ← «حجم خط الطباعة» ⇒ اكتب رقمًا (٧–١٨) واحفظ. جرّب ٨ أو ٨٫٥ لتقليص الصفحات، أو اتركه فارغًا للوضع التلقائيّ.

---

## ٣ · ما لم يتغيّر

- **الحسابات والأعمدة والتقارير دون مساس** — التغيير في حجم العرض للطباعة فقط.
- **الوضع الافتراضيّ (تلقائي) مطابقٌ للسابق** — لا انحدار في المطبوعات القائمة.
- **حجم الورق/الاتجاه/الترويسة/التذييل/العلامة المائية/الشعار** كما هي.

---

## ٤ · ما في الحزمة

| الملفّ | التغيير |
|---|---|
| `index.source.html` · `index.html` | دالّة `printFont(pc,ncols)` (auto/رقم)، تطبيقها في «مراجعة الفرع/المفصّل» و«الكتالوج» و«التقارير»، حشوٌ يتناسب مع الخطّ الصغير، الحقل «فارغ=تلقائي»، والافتراض `auto` |
| `build_harness.js` | (بلا تغيير في هذه الحزمة) |
| `test_printfit.mjs` | +٤ فحوص (P10–P13): الرقم الصريح مُطبَّق صعودًا/نزولًا، بلا قصّ، و«تلقائي» يعود للآليّ |
| `test_settings_center.mjs` | تحديث SC1 (الافتراض `auto`) |
| `README.md` · `.github/workflows/ci.yml` | تحديث العدّ (١٬٥١٦ ← ١٬٥٢١) |
| `تعليمات-تفعيل-حجم-خط-الطباعة.md` | هذه الوثيقة |

---

## ٥ · التركيب

1. خذ نسخةً احتياطيّة من `index.html`.
2. فُكّ الحزمة فوق جذر المستودع `dhtrd/iomp`.
3. `node build_release.js` — «الشيفرة متطابقةٌ حرفًا بحرف ✓».
4. `node build_harness.js` ثمّ `node run_all_tests.mjs` — **`TOTAL: 1521/1521 — ALL GREEN ✓`**.
5. ادفع إلى `main`.

**لا تغيير في أيّ حساب ولا قاعدة ولا صلاحيّة.**
